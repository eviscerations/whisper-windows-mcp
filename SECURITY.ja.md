# セキュリティポリシー

## スコープ

whisper-windows-mcpはローカルファーストのツールです。すべての音声処理はマシン上で行われます — 音声・動画ファイルや個人データが外部サーバーに送信されることは一切ありません。攻撃対象領域は以下に限定されます：

- ローカルファイルシステム（ツールに渡されるファイルパス）
- whisper-cli.exeバイナリとその依存関係
- Claude Desktop MCP接続（ローカルIPCのみ）
- ツールレスポンスで返されるトランスクリプトテキスト（以下のプライバシーアーキテクチャを参照）

## プライバシーアーキテクチャ

**音声ファイルはマシンの外に出ることは一切ありません。** この保証は無条件です。

**トランスクリプトテキストはマシンの外に出る可能性があります**（標準モードの場合）。ツールレスポンスにトランスクリプトテキストが含まれる場合、そのテキストはClaudeのAPIで処理されます。これは標準的なMCPの動作ですが、規制対象または機密性の高いコンテンツを扱うユーザーにとって、ツールの「ローカルファースト」の設計思想と実際のデータフローの間にギャップが生じます。

**プライバシーモード**（`WHISPER_PRIVACY_MODE=true`またはper-callで`privacy_mode=true`）は、すべてのツールレスポンスをメタデータのみに制限します — トランスクリプトテキストがClaudeのAPIに返されることは一切ありません。これは医療、法律、財務、および企業向けデプロイメントに適した設定です。

**プライバシーモードゲート：** プライバシーモードが有効な場合、すべての文字起こし操作の前に、操作ごと（ツール＋引数）にキー化された明示的な確認開示が表示されます。サーバーは*ブロック*を強制します — 特定の操作を初めて認識した時点で、その操作を保留し開示を返します。ただし、人間が回答したことを強制するものでは**ありません**：同一の呼び出しが再発行されると、ホストが開示を提示しユーザーが「はい」と回答したという前提のもと、ゲートは解除されます。人間を介在させずに同じ呼び出しを再発行するクライアントは、ゲートを自身で満たすことができます。これは暗号学的な障壁ではなく、MCPホストが開示を尊重することに依存する手続き的なインフォームドコンセント制御として扱ってください。

**同意ゲート：** 標準モードでは、セッション内で初めてトランスクリプトテキストがAPIに返される前に、一度だけセッション開示が表示されます。機密性のないコンテンツに対してこれを抑制するには、設定に`WHISPER_CONSENT_ACKNOWLEDGED=true`を設定してください。これは*セッションごとに一度*のゲートである点に注意してください：最初に確認されたトランスクリプト以降、同一セッション内の後続のトランスクリプトは再プロンプトなしで返されます。セッションの状態に関わらずAPIに決して到達してはならないコンテンツには、プライバシーモードを使用してください。

完全なプライバシーアーキテクチャの説明、コンプライアンスフレームワークのガイダンス（HIPAA、GDPR、弁護士・依頼者特権、FERPA、SOX、PCI-DSS）、および設定手順については[PRIVACY.md](PRIVACY.md)を参照してください。

## バイナリ検証

プリビルドリリースの`whisper-cli.exe`バイナリの整合性を確認するには、PowerShellでSHA256ハッシュを確認してください：

```powershell
Get-FileHash "C:\whisper\Release\whisper-cli.exe" -Algorithm SHA256
```

各リリースバイナリの期待されるハッシュは[リリースページ](https://github.com/eviscerations/whisper-windows-mcp/releases)に公開されています。ハッシュが一致しないバイナリは使用しないでください。

## サポートバージョン

セキュリティ修正は最新の公開バージョンのみに適用されます。

| バージョン | サポート |
|---|---|
| 2.x（最新） | ✅ |
| 1.x | ❌ |

## 脆弱性の報告

**セキュリティ上の脆弱性には公開Issueを使用しないでください。**

GitHubのプライベート脆弱性レポートを使用してください：
1. [Securityタブ](https://github.com/eviscerations/whisper-windows-mcp/security)にアクセス
2. 「Report a vulnerability」をクリック
3. 再現できる十分な詳細を含めて問題を説明

7日以内に返答します。脆弱性が確認された場合、できる限り早急に修正をリリースし、ご希望の場合はリリースノートにクレジットを記載します。

## サンドボックスと承認

whisper-windows-mcpは、**Claude Desktopを通じてマシンの所有者が操作する、ローカルでシングルユーザーのツールです。** その脅威モデルは、所有者が自分自身のマシン上で実行することであり — 信頼できない、マルチテナント、またはネットワークに公開された配備ではありません。

- **サンドボックス：** 設計上、ありません。`whisper-cli.exe`は、他のローカルMCPサーバーと同様に、所有者自身の権限レベルで実行されます。ここでの緩和策はOSレベルの分離ではなく、使用範囲です — **このサーバーを信頼できないネットワークアクセスに公開しないでください**（下記の「ファイルパスインジェクション」を参照）。
- **承認はサンドボックスベースではなく、階層化されています：**
  1. **ホストの承認** — Claude DesktopのMCPレイヤーがツールの呼び出しを制御します。
  2. **同意 / プライバシーゲート** — トランスクリプトテキストがマシンからClaudeのAPIへ送信される前に、明示的な確認が必要です。`WHISPER_PRIVACY_MODE` / per-callの`privacy_mode`は、規制対象コンテンツに対してメタデータのみを返します。ゲートは操作ごと（ツール＋引数）にキー化されます。[PRIVACY.md](PRIVACY.md)を参照してください。
  3. **入力検証** — パスまたはIDを受け取るすべてのツールで防御的に適用されます：
     - ディレクトリトラバーサル（`..`）およびUNC（`\\server\share`）パスは、`analyze_media`と`transcribe_batch`を含む**すべて**のファイル/フォルダ入力で拒否されます（後者2つは以前は存在確認のみでした — 検証されていないUNCパスは攻撃者のホストへの外向きSMB接続を誘発する可能性がありました）。
     - `job_id` / `batch_id`は、いかなるファイルシステムパスの構築に使用される前にも、サーバーが生成する正確な形式と照合されるため、細工されたIDでjobsディレクトリの外へトラバースし、任意のファイルの読み取り/書き込み/削除を行うことはできません。
     - `switch_model`**および**`transcribe_audio`の`model`上書きは、いずれも正規化されたパス封じ込めによって設定されたモデルディレクトリに封じ込められます — この上書きを使って任意のファイルをモデルとして`whisper-cli`に渡すことはできません。
     - `vad_model`パスはトラバーサル/UNCを拒否します。
     - `download_model`は、信頼できるHugging Faceネームスペースのアローリストに制限されます（初回URLとすべてのリダイレクト）。
     - サーバーが暗黙的に呼び出すWindowsシステムバイナリ（`tasklist`、`wmic`）は、絶対`System32`パスで呼び出されるため、`PATH`上でより早い位置に配置された同名の実行ファイルによってシャドウイングされることはありません。

**「信頼できないエージェント」の境界に関する注記。** このツールは、共有またはネットワークに公開されたインフラとしてではなく、Claude Desktopを通じて操作する単一の所有者向けに設計されています。しかし、文字起こしされた音声・動画コンテンツ自体は、*指示に似ている*可能性があり、次にどのツールがどの引数で呼び出されるかに影響を与えうる信頼できない入力です（下記の「トランスクリプトインジェクション」を参照）。そのため、上記の入力検証は、シングルユーザーの前提のみに頼るのではなく防御的に適用されています。完全に信頼できないエージェントやマルチテナントの体制では、依然としてOS/コンテナのサンドボックスとエグレスポリシーが必要であり — ローカルでシングルユーザーの文字起こしツールの範囲外です。

## 既知の設計上の決定

- **ファイルパスインジェクション：** ツールはClaudeから絶対ファイルパスを受け取ります。これは意図的な設計です — このツールはマシンの所有者がClaude Desktopとともに使用することを目的としています。トラバーサル（`..`）およびUNCパスは、パスを受け取るすべてのツールで拒否されます；それ以外の絶対ローカルパスは受け付けられます。このMCPサーバーを信頼できないネットワークアクセスに公開しないでください。
- **ジョブ/バッチID検証：** `job_id`と`batch_id`は、いかなるファイルシステムパスの構築に使用される前にも、サーバーが生成する正確な形状（`job_<epochMs>_<8 hex>` / `batch_<epochMs>_<8 hex>`）と一致しなければなりません。これにより、細工されたIDがジョブ完了処理を介してjobsディレクトリの外へトラバースし、任意のファイルの読み取り、書き込み、または削除を行うことを防ぎます。
- **同意/プライバシーゲートは手続き的です：** ゲートは、MCPホストが開示を提示し、操作が再発行される前に人間が回答することに依存します。サーバーは「再発行されるまでブロックする」動作を強制しますが、人間が回答したことを検証することはできません。APIに決して到達してはならないコンテンツには、ゲート単独ではなくプライバシーモード（メタデータのみのレスポンス）に頼ってください。
- **サンドボックスなし：** whisper-cli.exeはClaude Desktopと同じ権限で実行されます。これはローカルMCPツールの標準的な動作です。
- **一時ファイル：** 中間WAVファイルは`%TEMP%\whisper_tmp_*.wav`に書き込まれ、文字起こし後に削除されます。ジョブ状態ファイルは`%TEMP%\whisper-mcp-jobs\`に書き込まれ、サーバー起動時に7日以上経過したファイルが自動的にクリーンアップされます。
- **トランスクリプトコンテンツ：** 標準モードでツールレスポンスに返されるトランスクリプトテキストはClaudeのAPIで処理されます。これを防ぐには`WHISPER_PRIVACY_MODE=true`を有効にするか、per-callで`privacy_mode=true`を渡してください。詳細は[PRIVACY.md](PRIVACY.md)を参照してください。
- **トランスクリプトインジェクション：** 音声ファイルには、文字起こしされると指示に似た内容が含まれる可能性があります。Claudeの組み込み防御機能がこれを処理します。MCPサーバー自体はすべてのトランスクリプトコンテンツを信頼できないデータとしてマークし、指示として解釈することは一切ありません。
- **モデルダウンロード：** `download_model`ツールは2つの信頼できるHugging Faceネームスペース（`ggerganov/whisper.cpp`と`ggml-org`）からのみダウンロードします。リダイレクトはアローリストに対して検証されてから追跡されます。任意のURLはコードレベルで拒否されます。切り詰められた/不完全なダウンロードは、`.part`ファイルがモデル名に昇格される前に拒否されます（Content-Lengthチェック）。**フォローアップ：** ダウンロードはまだモデルごとのSHA256ダイジェストで検証されていないため、侵害された上流や経路上の攻撃者が悪意のある`.bin`を提供する可能性は依然として残ります。ダイジェストのピン留めが計画されています；高保証のデプロイメントでは、リリースページのハッシュと手動で照合してください。
- **モデル選択の封じ込め：** `switch_model`と`transcribe_audio`の`model`上書きの両方は、設定されたモデルディレクトリ内の`.bin`ファイルのみを受け付けます。そのディレクトリ外のパスは、正規化されたパス封じ込めによって拒否されます — `…\models-evil`のような兄弟プレフィックスのディレクトリはチェックを満たせません — パスの指定方法に関わらず拒否されます。`vad_model`パスはトラバーサル/UNCを拒否します。
- **暗黙的なシステムバイナリ：** `tasklist`と`wmic`は、素の名前ではなく絶対`System32`パスで呼び出されるため、`PATH`上でより早い位置に配置された同名の実行ファイルによってシャドウイングされることはありません。
- **永続モデルサーバー：** オプションの`whisper_server`ツールは、whisper.cppの`whisper-server`を常駐プロセスとして実行します。`127.0.0.1`のみにバインドされる — ルーティング可能なインターフェースには決してバインドされない — ため、マシンの外からは到達できません。明示的に起動および停止され（自動起動は決してされません）、所有するプロセスはシャットダウン時にkillされます。常駐サーバーとワンショットの`whisper-cli`は同じGPU/VRAMを奪い合うため、両者は相互排他的です：デタッチドスポーンパスのハードなバックストップが、サーバー起動中にCLIジョブが起動するのを防ぎ、文字起こしツールはサーバーが停止されるまでCLIを必要とする操作を拒否します。`WHISPER_SERVER_PORT`はlocalhostポートを選択します；ホストは設計上、設定できません。
