# whisper-windows-mcp — ロードマップ

現在のバージョン：**v2.0.0**

---

## 設計原則

これらの原則はこのプロジェクトのすべての決定を支配し、機能追加の速度より優先されます。

**Claude APIの使用を最小限に。** スキャン、分析、キュー管理、実行、検証、モデル切り替えを含むワークフロー全体を、できる限り少ないClaude操作で完了できなければなりません。このツールはClaudeのProまたはMaxサブスクリプションを契約していないフリープランユーザーでも完全に機能する必要があります。すべてのツール呼び出しは使用量を消費します。この原則を念頭に設計してください。

**常に1つのwhisperインスタンス。** 実行中に2つ目のwhisper-cli.exeプロセスを生成しない。プロセスロックは必須であり、例外はありません。

**ローカルファースト、デフォルトでプライベート。** 音声はマシンから外に出ない。コア機能にクラウドAPIは不要。オプションの統合機能（例：Hugging Faceからのモデルダウンロード）は明確にオプションとして文書化する必要があります。

**明示的なユーザー制御。** サイレントな一括操作なし。破壊的または不可逆なアクションは確認が必要。実行前にユーザーが何が起こるかを常に把握できるようにする。

**Unicodeセーフなパス処理。** すべてのファイルI/Oで、日本語、中国語、絵文字、括弧などの非ASCII文字を含むファイル名を正しく処理する必要があります。

**モジュール型かつ組み合わせ可能。** ツールは独立しています。ユーザーは必要なものだけを使います。やむを得ない場合を除き、どの機能も他の機能を必要としてはなりません。

**機能追加より最適化を優先。** 機能追加とシステム負荷またはAPI呼び出し回数の削減の間で迷った場合は、負荷を削減してください。大規模な最適化作業はコストがかかります。最初から正しいアーキテクチャを設計してください。

---

## 完了済み

### ✅ v1.3.1 — プロセスロック
`tasklist /FI`を使用した`isWhisperRunning()`チェックを追加。競合するプロセスを生成する代わりに、明確なエラーとTask Managerの手順を返します。

### ✅ v1.4.0 — Vulkan GPU加速
VS Build Tools 2026とVulkan SDK 1.4.341.1を使用して`-DGGML_VULKAN=ON`でwhisper.cppをコンパイル。ビルド済みVulkanバイナリを`whisper-vulkan-win-x64.zip`として配布。

**AMD Radeon RX Vega 56での結果：** GPU使用率約16%。58分のファイルがGPUで約4.5分（CPU専用では約88分）で完了。

### ✅ v1.5.0 — システム診断
`check_system`ツール：`wmic`によるGPU検出、Vulkan DLL確認、VRAM報告、モデルサイズ推奨。

### ✅ v1.6.0 — メディアファイル事前分析
FFprobeを使用した`analyze_media`ツール：時間、サイズ、コーデック、文字起こし状態、CPUとGPUの推定時間。単一ファイルまたはフォルダスキャン、ソートオプション付き。

### ✅ v1.7.0 — バックグラウンド文字起こし + 進捗表示
デタッチドプロセスアーキテクチャ：`background=true`の`transcribe_audio`がwhisperをデタッチドプロセスとして起動し、即座にジョブIDを返す。`check_progress`がwhisperのstderrセグメントタイムスタンプをリアルタイムで解析。

### ✅ v1.8.0 — 検証付き順次バッチ処理
`start_batch`と`check_batch_progress`：自動順次処理、文字起こし検証（空または短すぎる出力の検出）、自動キュー進行、ファイルごとの進捗タイムスタンプ。

### ✅ v1.9.0 — 多言語対応と翻訳
`language=auto`による言語自動検出と`translate_to_english=true`によるデュアルSRT出力。`.3gp`と`.ts`フォーマットのサポートを追加。`language=auto`は`transcribe_audio`でも利用可能。

**既知の制限：** Whisperの組み込み翻訳は英語へのみ対応。英語以外の言語には`large-v3`モデルが必要 — 英語専用モデル（`*.en.bin`）は英語以外の音声に`[FOREIGN]`を出力します。

### ✅ v2.0.0 — Unicodeセーフパス + バックグラウンドSRT
**Unicodeファイル名：** 非ASCII文字を含むファイル名でバックグラウンド文字起こしがサイレントに失敗する問題を修正。ジョブIDベースのサニタイズされた一時パスに出力を書き込み、完了後に正しい宛先に移動するよう変更。

**バックグラウンドモードでのSRT：** `spawnDetached`が要求されたフォーマットに関わらず`-otxt`をハードコードしており、`generate_subtitles`が同期的にブロックして4分を超えるファイルでMCPタイムアウトが発生していた問題を修正。`spawnDetached`に`outputFormat`パラメータを追加し、バックグラウンドモードで`text`と`srt`出力をサポート。

---

## 対応予定 — v2.0.1：バグ修正

v2.0.0コードベースで確認されたバグです。新機能を追加する前にパッチを適用します。

### buildArgsにハルシネーション防止が未実装
`buildArgs`内のどこにも`--condition-on-previous-text`が渡されていません。これがないと、whisperは各セグメントを自身の前の出力を条件として処理するため、低品質な音声・無音・バックグラウンドノイズで暴走するハルシネーションループが発生します（「ありがとうございます。ありがとうございます。」のような繰り返し出力になります）。

**修正内容：** `buildArgs`のデフォルトとして`--condition-on-previous-text 0`をハードコード。長尺音声で最も重要な品質フラグであり、省略してはなりません。また`--no-speech-thold`をオプションパラメータ（デフォルト`0.6`）として公開し、信頼度閾値以下のセグメントをハルシネーションではなく無音として扱うようにします。

### TROUBLESHOOTING.mdのCLIバッチコマンドが機能しない
TROUBLESHOOTING.mdに記載されている大規模バッチコマンドは、FFmpeg変換なしにMP4ファイルを直接whisper-cliに渡しています。whisper-cliはMP4をWAVとして読み込めないため、すべてのファイルで`error: failed to read audio data as wav`が発生します。

**修正内容：** 正しいFFmpeg変換アプローチに置き換えます。また`Start-Process -RedirectStandardOutput`をwhisper出力をキャプチャするための正しいPowerShell方式として文書化します — whisperはトランスクリプトをstdoutに、診断情報をstderrに書き込むため、`|`でパイプして`2>$null`でstderrを抑制しても何もキャプチャされません。

`TROUBLESHOOTING.md`と`TROUBLESHOOTING.ja.md`の両方に適用されます。

### ツール出力全体でのファイル名参照
すべてのツール出力で、位置インデックスではなく完全なソースファイル名を参照するようにします。バッチ一覧、進捗レポート、エラーメッセージに影響します。すべてのツールハンドラーの完全な監査が必要です。

---

## 予定 — v2.1.0：モデル管理スイート

最優先の機能ブロックです。現在ユーザーはモデルファイルを手動で検索・ダウンロード・設定する必要があり、起動時にアクティブモデルが固定されます。これは大きな摩擦を生み、セッション中の柔軟性を妨げます。3つのツールはまとめてリリースします。

### `list_models`
設定済みモデルディレクトリをスキャンし、インストール済みWhisperモデルファイルの一覧を返します。各モデルについて：ファイル名、ディスク上のサイズ、現在アクティブかどうか、量子化レベル（該当する場合）、推奨用途を表示します。ネットワーク接続不要。ローカルファイルシステムのみ読み取り。1回のツール呼び出し、1回のレスポンス。

### `download_model`
Hugging Faceから設定済みモデルディレクトリにモデルファイルを直接取得します。モデル名を受け取り、正しいダウンロードURLを自動的に解決します。フル精度モデルと量子化モデルの両方をサポートします。ファイルサイズを報告し、完了後に有効性を確認します。

Node.js組み込みの`https`または`fetch`（最小要件のNode 18+で利用可能）を使用して実装。新しい外部ランタイム依存関係は追加しません。

**対応モデル（フル精度）：** `tiny.en`、`base.en`、`small.en`、`medium.en`、`large-v3`、`large-v3-turbo`、および多言語バリアント。

**対応モデル（量子化）：** `base.en-q5_1`、`small.en-q5_1`、`medium.en-q5_0`、`large-v3-q5_0`、`large-v3-turbo-q5_0`、その他のGGML量子化バリアント。量子化モデルはフル精度より40〜70%小さく、精度損失は最小限で、CPUでの推論が大幅に高速です — GPU加速なしのユーザーに強く推奨します。

### `switch_model`
Claude Desktopを再起動したり設定ファイルを編集したりせずに、現在のセッションのアクティブモデルを変更します。ファイルの存在を確認し、文字起こしが実行中でないことを確認（プロセスロックを強制）してから、メモリ内のアクティブモデルを更新します。セッションスコープ — 設定には保存されません。

**実装：** `WHISPER_MODEL`を`const`からモジュールレベルの`let`に変更。`switch_model`が直接更新します。次の文字起こしから新しいモデルが使用されます。

### `large-v3-turbo`の文書化
`large-v3-turbo`は`large-v3`の蒸留バリアントで、英語の会話コンテンツでの精度損失を最小限に抑えながら約6倍高速です。GPUが利用可能な英語バッチ処理の推奨モデルとして文書化します。`large-v3-turbo-q5_0`はCPUのみで多言語サポートが必要なユーザーへの推奨オプションです。

更新が必要な箇所：READMEとREADME.ja.mdのモデル一覧、`check_system`の推奨事項、`list_models`のユースケース説明、`download_model`の対応モデル一覧。

---

## 予定 — Bunへの移行

v2.2.0の機能追加前に、ランタイムをNode.jsから[Bun](https://bun.sh)に移行します。

Claude Desktopはセッション起動のたびにMCPサーバーを新しく生成するため、起動時間はクリティカルパスにあります。BunはTypeScriptをネイティブにコンパイルステップなしで実行し、Node.jsより大幅に高速に起動し、I/Oも高速です — これらはすべてこのユースケースに直接メリットをもたらします。

**変更点：**
- `tsc`のビルドステップと`dist/`ディレクトリを完全に削除
- ユーザーがTypeScriptソースを直接実行
- `tsconfig.json`がオプション/簡略化される
- `package.json`スクリプトを更新
- npm公開ワークフローを更新

**変更しない点：**
- `src/index.ts`のソースコード — Bunは既存のTypeScriptとNode.js組み込みAPIと互換性があります
- すべてのツールの動作と出力形式
- エンドユーザーのClaude Desktop設定

**v2.2.0より前に移行する理由：** コードベースは今が最も移行しやすい状態です。ツールを追加した後では移行の作業量が増えるだけでメリットはありません。

---

## 予定 — v2.2.0：品質・パラメータ・ハードウェアの拡張

### `temperature`パラメータ
`transcribe_audio`と`generate_subtitles`にwhisperの`--temperature`フラグを公開します。範囲0.0〜1.0。値が高いほどセグメントデコードにバリエーションが生まれ、一貫性を犠牲にしてノイズの多い音声でのハルシネーションを減らせます。デフォルト：`0.0`（決定論的）。低い実装コスト — 単一CLIフラグのパススルー。

### `prompt`パラメータ
`transcribe_audio`と`generate_subtitles`にwhisperの`--prompt`フラグを公開します。文字起こし開始前に事前コンテキストとして挿入される文字列を受け取ります。ドメイン固有の語彙、話者名、スタイルコンテキストに有用。特殊なコンテンツの精度を大幅に向上させる可能性があります。

例：`"話者：Keemstar、DramaAlert。これはストリーミング解説の録音です。"`

低い実装コスト — 単一CLIフラグのパススルー。

### `--condition-on-previous-text`のユーザーパラメータ化
v2.0.1で`0`にハードコードされます。コンテキストの継続性が役立つ構造化された音声など、特定のユースケースでコンディショニングを再有効化したい上級ユーザー向けにオプションのブール値として公開します。デフォルトは`0`のまま。

### `word_timestamps`フラグ
`--max-len 1`と`--split-on-word`を組み合わせて単語レベルのタイムスタンプ出力を実現します。JSONパースを必要とせず、標準のタイムスタンプ形式で1単語ごとにセグメント化されたトランスクリプトを返します。当初計画していた`-oj` JSONアプローチより実装がはるかにシンプルです。

`--max-len`パラメータは`max_segment_length`として独立したオプションとしても公開します（字幕の読みやすさのために長いセグメントを制御したいユーザー向け）。

### 音声アクティビティ検出（VAD）
whisperの`--vad`フラグをモデル管理ワークフローと統合して公開します。VADは音声セグメントを検出して無音部分とノイズを除去した後、音声のみをwhisperに渡す軽量なSileroモデルで音声を前処理します。効果：文字起こしの高速化（音声部分のみを処理）と大幅なハルシネーション削減（whisperが無音をハルシネーションする機会がなくなる）。

**実装：** 別途VADモデルファイル（`ggml-silero-v5.1.2.bin`または最新版）が必要で、`huggingface.co/ggml-org/whisper-vad`からダウンロードします。このモデルは`download_model`でサポートされる対象として追加されます。VADが有効な場合、`--vad-model`パスが`--vad`とともに渡されます。

**VADはWhisper.cpp v1.8.3（2026年1月）以降、Windowsで安定して動作することが確認されており**、バッファオーバーフロー修正がv1.8.4（2026年3月）でリリースされています。v1.8.4以降のバイナリを推奨します。

これはこのマイルストーンで最もインパクトの大きい品質向上機能です — 長尺または音質の悪いファイルのバッチ処理に強く推奨します。

### 組み込みステレオダイアライゼーション（`--diarize`）
whisperのネイティブ`-di` / `--diarize`フラグを公開します。話者が別々のチャンネル（左/右）に録音されたステレオ音声ファイルに対して、話者に属性付けされたトランスクリプトセグメントを出力します。追加の依存関係なし、追加モデル不要 — 純粋なCLIフラグのパススルー。

将来のリリースで予定しているpyannoteベースの話者識別（モノラル録音に対応、別途Pythonライブラリが必要）とは異なります。ステレオコンテンツ向けの軽量・ゼロコストのオプションです。

### 時間範囲の文字起こし（`--offset-t`、`--duration`）
`transcribe_audio`に`--offset-t`（ミリ秒単位の開始オフセット）と`--duration`（ミリ秒単位の処理時間）を公開します。ファイル全体を処理せずに特定の時間ウィンドウのみを文字起こしできます。長尺ファイルの問題箇所の再処理、出力の抜き打ち確認、特定のセグメントの抽出に有用です。

### `--beam-size` / `--best-of`パラメータ
whisperのビームサーチコントロールを公開します。`--beam-size`（デフォルト5）は探索の幅を制御します — 値が高いほど速度を犠牲にして精度が向上します。`--best-of`（デフォルト5）は評価する候補シーケンスの数を制御します。重要なファイルで処理時間と引き換えに精度を上げたいユーザーに有用です。低い実装コスト — CLIフラグのパススルー。

### `--gpu-device`パラメータ
whisper.cpp v1.8.4で追加された`-g` / `--gpu-device`フラグを公開します。複数GPUを持つユーザーが使用するデバイスをインデックスで指定できます。`check_system`の出力に利用可能なGPUリストを表示し、どのインデックスがどのデバイスに対応するかをユーザーが把握できるようにします。統合GPUと外付けGPUの両方を持つシステムに特に関係します。

### `--processors`パラメータ
複数CPUプロセッサによる並列チャンク処理のための`-p` / `--processors`フラグを公開します。スレッド数だけでは達成できない速度向上をマルチコアシステムにもたらす可能性があります。低い実装コスト。

---

## 予定 — v2.3.0：出力フォーマットの拡張

### VTT字幕フォーマット
SRTに加えてWebVTT（`.vtt`）出力をサポート。VTTはYouTube、HTML5 `<video>`、ほとんどの現代的なプレーヤーで使用されるウェブ標準です。whisper-cliはネイティブにサポートしています。`transcribe_audio`、`generate_subtitles`、`spawnDetached`に`vtt`を有効な出力フォーマットとして追加。`buildArgs`と関連するすべてのツールスキーマ、README、日本語ドキュメントを更新します。

### LRCフォーマット
`-olrc`によるLRC（`.lrc`）歌詞/カラオケフォーマット出力。メディアプレーヤーによる同期歌詞表示に使用されます。ニッチなユースケースですが実装コストはゼロ — ネイティブCLIフラグです。

### CSVフォーマット
`-ocsv`によるCSV（`.csv`）出力。セグメントタイミング付きの構造化表形式データ — 下流分析、クリップ位置合わせワークフロー、スプレッドシートツールへのインポートに有用です。実装コストはゼロ — ネイティブCLIフラグです。

---

## 予定 — 将来のリリース

### TinyDiarize
`tdrz`対応モデルバリアント（例：`large-v2-tdrz`）での`--tinydiarize`フラグサポート。ステレオ専用の`--diarize`フラグとは異なり、TinyDiarizeはモノラル録音に対応します。特別なモデルバリアントのダウンロードが必要です。pyannoteベースのダイアライゼーションより精度は低いですが、モデルファイル以外の追加依存関係はゼロです。

**状況：** 計画中。`download_model`がtdrzモデルバリアントをサポートすることに依存します。

### YouTube URL文字起こし
yt-dlp経由でYouTube URLから直接文字起こし。音声のダウンロードと文字起こしを1ステップで実行。yt-dlpのインストールとPATH設定が必要。

**設計制約：** yt-dlpはオプション。見つからない場合は明確なインストール手順とともに適切にエラー処理する必要があります。このツールを必要としないユーザーのコア機能への変更なし。

### 動画プロジェクトワークフローツール
ソースクリップと編集済みクリップのディレクトリを管理するユーザー向け：

1. ソースディレクトリとクリップサブディレクトリをスキャン
2. 編集済みクリップのトランスクリプトをソーストランスクリプトにファジーマッチして、ソース内の位置を特定
3. トランスクリプトコンテンツに基づいてClaudeが提案する説明的なファイル名を表示（リネーム実行前に明示的なユーザー確認が必要）
4. タイムコード結果付きでプロジェクトディレクトリ全体のトランスクリプトを検索

**設計制約：**
- ソースファイルは**絶対にリネームまたは変更しない**
- すべてのリネームには**明示的なユーザー確認**が必要
- 検索はスタンドアロンツールとして独立して使用可能
- 分析とマッチングはローカルで実行 — Claudeはユーザーが結果をレビューするときのみ呼び出され、API呼び出しを最小化

**状況：** 設計フェーズ。実際のディレクトリ構造の例をもとに実装予定。

### 話者識別（pyannote-audio）
話者IDラベル付きの完全なモノラル話者識別 — チャンネル構成に関わらず、録音全体の話者の切り替えをマークします。組み込みの`--diarize`ステレオフラグ（v2.2.0）およびTinyDiarizeとは異なります。

**実装：** [pyannote-audio](https://github.com/pyannote/pyannote-audio)が必要 — Hugging Faceモデルアクセストークンが必要なPythonベースのライブラリ。whisper.cppパイプラインとは完全に別の依存関係スタック。

**状況：** 独自のセットアップドキュメントを持つオプションの高度機能として計画中。メインパッケージには含めません。

### 英語以外の言語への翻訳
Whisperの`--translate`フラグは英語のみを対象としています。任意のターゲット言語をサポートするには、外部翻訳APIまたはローカル翻訳モデルが必要です。

**検討中のオプション：** LibreTranslate（セルフホスト可能、ローカルファースト）、ローカルLLM翻訳、または明示的なスコープ外ドキュメント。

**状況：** ローカルファーストかAPIへの依存かの設計判断待ちで保留中。

### 文字起こしのクリーンアップと整形
後処理パイプライン：
- フィラーワードと言い直しの削除（オプション、ユーザー制御）
- 自然なトピック境界での段落区切り
- ダイアライゼーションと組み合わせた話者を考慮した整形
- PDFまたはDOCXへのエクスポート

**状況：** 計画中。話者を考慮したバリアントはダイアライゼーションに依存します。

---

## 配信

[npm](https://www.npmjs.com/package/whisper-windows-mcp)、[mcpservers.org](https://mcpservers.org)、[Glama](https://glama.ai)で公開中。

---

## 多言語ドキュメント

日本語ドキュメントは英語と並行して管理されます。各リリース後に英語ドキュメントに合わせて以下のファイルを更新する必要があります：

- `README.ja.md`
- `TROUBLESHOOTING.ja.md`
- `ROADMAP.ja.md`

他の言語へのコミュニティ貢献を歓迎します。

---

## コントリビュート

プルリクエスト歓返です。作業を開始する前に既存のIssueを確認してください。

上記以外のハードウェアでGPU加速をテストした方は、GPU型番、VRAM、使用モデル、確認したスループットをIssueで報告してください。他のユーザーへの正確なパフォーマンス参考情報の構築に役立ちます。
