# whisper-windows-mcp — ロードマップ

現在のバージョン：**v2.5.0**

---

## 設計原則

これらの原則はこのプロジェクトのすべての決定を支配し、機能追加の速度より優先されます。

**Claude APIの使用を最小限に。** 文字起こしワークフロー全体 — スキャン、分析、キュー投入、実行、検証、モデル切り替え — を、できる限り少ないClaude操作で実行できなければなりません。このツールは、ProやMaxのサブスクリプションを契約していないフリープランのClaudeユーザーでも完全に機能する必要があります。すべてのツール呼び出しは使用量の予算を消費します。それを踏まえて設計してください。

**常に1つのwhisperインスタンス。** 実行中に2つ目のwhisper-cli.exeプロセスを決して生成しない。プロセスロックは必須であり、交渉の余地はありません。

**ローカルファースト、デフォルトでプライベート。** 音声がマシンの外に出ることは決してありません。コア機能にクラウドAPIは不要です。オプションの統合機能（例：Hugging Faceからのモデルダウンロード）は、オプションであることを明確に文書化する必要があります。

**明示的なユーザー制御。** サイレントな一括操作はありません。破壊的または不可逆なアクションには確認が必要です。何が起ころうとしているのかを、それが起こる前にユーザーが常に把握できなければなりません。

**Unicodeセーフなパス。** すべてのファイルI/Oは、日本語、中国語、絵文字、括弧、その他の特殊文字を含む非ASCIIのファイル名を正しく処理する必要があります。

**モジュール型かつ組み合わせ可能。** ツールは独立しています。ユーザーは必要なものだけを使います。やむを得ない場合を除き、どの機能も動作のために他の機能を必要とすべきではありません。

**機能より最適化を優先。** 機能を追加するか、システム負荷やAPI呼び出し回数を削減するかで迷った場合は、負荷を削減してください。大規模な最適化作業はコストがかかります。最初からアーキテクチャを正しく設計してください。

---

## 完了済み

### ✅ v1.3.1 — プロセスロック
文字起こしのspawn前に`tasklist /FI`を用いた`isWhisperRunning()`チェックを追加。競合するプロセスを生成する代わりに、明確なエラーとTask Managerの手順を返します。

### ✅ v1.4.0 — Vulkan GPU加速
VS Build Tools 2022とVulkan SDKを用いて`-DGGML_VULKAN=ON`でwhisper.cppをソースからコンパイル。ビルド済みのVulkanバイナリを`whisper-vulkan-win-x64.zip`として配布。

**AMD Radeon RX Vega 56での結果：** GPU使用率は平均約16%。58分のファイルがGPUで約4.5分、CPU専用では約88分で完了します。

### ✅ v1.5.0 — システム診断
`check_system`ツール：`wmic`によるGPU検出、Vulkan DLLの検証、VRAMの報告、モデルサイズの推奨。

### ✅ v1.6.0 — ファイル事前分析
FFprobeを用いた`analyze_media`ツール：長さ、サイズ、コーデック、文字起こし状態、CPUおよびGPUの推定時間。単一ファイルまたはフォルダのスキャンに対応し、ソートオプション付き。

### ✅ v1.7.0 — バックグラウンド文字起こし + 進捗の可視化
デタッチドプロセスアーキテクチャ：`background=true`の`transcribe_audio`がwhisperをデタッチドプロセスとして生成し、即座にジョブIDを返します。`check_progress`がwhisperのstderrのセグメントタイムスタンプを解析し、リアルタイムのパーセンテージとETAを算出します。

### ✅ v1.8.0 — 検証付きの順次バッチ処理
`start_batch`と`check_batch_progress`：自動的な順次処理、トランスクリプトの検証（空または短すぎる出力の検出）、自動的なキューの進行、ファイルごとの進捗タイムスタンプ。

### ✅ v1.9.0 — 多言語対応と翻訳
`language=auto`検出と`translate_to_english=true`によるデュアルSRT出力を備えた`generate_subtitles`。`.3gp`と`.ts`フォーマットのサポートを追加。`language=auto`は`transcribe_audio`でも利用可能です。

**既知の制限：** Whisperの組み込み翻訳は英語のみを対象とします。英語以外の言語には`large-v3`モデルが必要です — 英語専用モデル（`*.en.bin`）は英語以外の音声に対して`[FOREIGN]`を出力します。

### ✅ v2.0.0 — Unicodeセーフなパス + バックグラウンドSRT
**Unicodeファイル名：** ファイル名に非ASCII文字を含むファイルで、バックグラウンド文字起こしがサイレントに失敗していました。すべての出力をサニタイズされたジョブIDベースの一時パスに通し、完了後に結果を正しい宛先へ移動することで修正しました。

**バックグラウンドモードでのSRT：** `spawnDetached`は、要求されたフォーマットに関わらず`-otxt`をハードコードしていました。`spawnDetached`に`outputFormat`パラメータを追加し、バックグラウンドモードで`text`と`srt`出力をサポートすることで修正しました。

### ✅ v2.0.1 — バグ修正（v2.2.0で出荷）
- `buildArgs`と`spawnDetached`の両方に`--max-context 0`をハードコード — 長尺音声でのハルシネーションループを防止します。
- `--no-speech-thold 0.6`を両関数にハードコード — 信頼度の閾値を下回るセグメントを、ハルシネーションされた内容ではなく無音として扱います。
- パス検証（`validateInputPath`）— UNCパスと`..`によるトラバーサルを拒否します。
- `MAX_FILE_SIZE_MB = 10240`のファイルサイズガード。
- `transcribeSingle`にトランスクリプトインジェクションのセキュリティコメントを追加。
- TROUBLESHOOTING.mdで壊れていたCLIバッチコマンドを修正。

### ✅ v2.1.0 — モデル管理スイート（v2.2.0で出荷）
- `WHISPER_MODEL`を`const`から`let`へ変更（セッション中に変更可能）。
- `MODEL_REGISTRY` — 16モデル、フル精度および量子化バリアント、Hugging FaceのダウンロードURL。
- `ALLOWED_HF_PREFIXES` — ダウンロードを`ggerganov/whisper.cpp`および`ggml-org`のネームスペースに制限するURLアローリスト。
- `list_models`ツール — モデルディレクトリをスキャンし、アクティブなモデル、サイズ、ユースケース、利用可能なダウンロードを表示します。
- `download_model`ツール — Node.jsの組み込み`https`を用いてHugging Faceからダウンロードし、アトミックにリネームします。
- `switch_model`ツール — `.bin`拡張子の検証、ディレクトリ制約、プロセスロックのチェック。
- `recommendedModel()`を更新し、6GB以上のVRAMに対して`large-v3-turbo`を推奨するように変更。

### ✅ v2.2.0 — 品質・パラメータ・ハードウェアの拡張
- `buildArgs`の位置引数を置き換える`WhisperOptions`インターフェース。
- `transcribe_audio`の新パラメータ：`temperature`、`prompt`、`condition_on_prev_text`、`no_speech_thold`、`beam_size`、`best_of`、`gpu_device`、`processors`、`word_timestamps`、`max_segment_length`、`split_on_word`、`diarize`、`vad_model`、`offset_t`、`duration`。
- `generate_subtitles`の新パラメータ：`temperature`、`prompt`、`beam_size`、`best_of`、`diarize`、`vad_model`。
- `spawnDetached`をリファクタリング — バックグラウンド/バッチモードですべての品質フラグが適用されます。
- バッチ出力の修正 — `readBatchProgress`が検証前に一時出力を最終宛先へ移動するようになりました。

**フラグ互換性の注意：** `gpu_device` / `--device`はwhisper.cpp v1.8.4で追加されました。リリースに含まれるビルド済みVulkanバイナリはv1.8.3世代です — このパラメータはツールで受け付けられますが、ユーザーがv1.8.4以降のバイナリに更新するまで効果はありません。

### ✅ v2.2.2 — パッチ
- デュアルライセンスの修正 — LICENSEとLICENSE-COMMERCIAL.mdを訂正。
- 軽微なドキュメントの訂正。

### ✅ v2.3.0 — バッチ自動進行、プライバシーアーキテクチャ、出力フォーマット拡張

**バッチ自動進行（重大なバグ修正）：** `start_batch`は、以前はキューを進めるために能動的なポーリングを必要としていました。生成された各whisper-cli子プロセスに`on('exit')`ハンドラーをアタッチするようになりました。プロセスが終了すると、exitコールバックを介してバッチが即座に自ら進行し、ポーリングのオーバーヘッドはゼロ、消費されるAPI呼び出しもゼロです。同時に発生する終了ハンドラーと`check_batch_progress`呼び出しの間で二重生成が起きないよう、ミューテックスで防止します。

**プライバシーアーキテクチャ：**
- `WHISPER_PRIVACY_MODE`環境変数 — `true`の場合、すべてのツールレスポンスはメタデータのみ（ファイル名、単語数、保存パス）を返します。トランスクリプトのテキストがClaudeのAPIに送信されることは決してありません。トランスクリプトはローカルファイルとしてのみ存在します。
- `WHISPER_CONSENT_ACKNOWLEDGED`環境変数 — `true`の場合、機密性のないコンテンツに対する一度限りのセッション同意ゲートを抑制します。
- `transcribe_audio`、`transcribe_batch`、`start_batch`、`check_progress`のper-call `privacy_mode`パラメータ。グローバルの環境変数をどちらの方向にも上書きします。per-callでの切り替えに再起動は不要です。
- プライバシーモードゲート（`checkPrivacyGate()`）— 有効なプライバシーモードがアクティブな場合、すべての操作の前に発火します。最初の呼び出しでアーム（開示を表示）し、2回目でクリア（許可）します。各操作の後にリセットされます。セッション同意ゲートとは完全に独立しています。
- セッション同意ゲート（`transcriptPolicy()`）— 標準モードで、トランスクリプトを返す最初の呼び出しの前にセッションごとに一度発火します。`sessionConsentGiven`フラグで消費されます。
- `PRIVACY.md` — HIPAA、GDPR、弁護士・依頼者間の秘匿特権、FERPA、SOX、PCI-DSS、NDA/企業秘密を網羅した完全なコンプライアンスドキュメント。
- トランスクリプトを返すすべてのツールの説明にプライバシー警告を追加。

**出力フォーマットの拡張：**
- `vtt` — `-ovtt`によるWebVTT字幕出力。`transcribe_audio`、`generate_subtitles`、`start_batch`、バックグラウンドモードで利用可能。
- `lrc` — `-olrc`によるLRC歌詞/カラオケフォーマット。`transcribe_audio`とバックグラウンドモードで利用可能。
- `csv` — `-ocsv`によるタイムスタンプ付きCSV。`transcribe_audio`とバックグラウンドモードで利用可能。
- `output_format`のデフォルトを、すべてのツールとコードパスで`"text"`から`"timestamps"`へ変更。プレーンテキストはオプトインになりました。

**バグ修正：**
- Bug 1：`output_format`がバックグラウンドジョブに転送されていませんでした — 要求されたフォーマットに関わらずデフォルトの`"text"`が使われていました。デフォルトを`"timestamps"`に変更し、正しく転送することで修正しました。
- Bug 2：バックグラウンドジョブの出力移動操作におけるサイレントな`catch {}`が失敗を握りつぶしていました。移動後に明示的な`existsSync`チェックと詳細な失敗メッセージを追加しました。
- Bug 3：非プライバシーのバックグラウンドジョブで、同意ゲートが意図的に`check_progress`へ延期される理由を文書化する設計コメントを、バックグラウンド生成箇所に追加しました。

**その他：**
- 一時ディレクトリの自動クリーンアップ — `cleanupOldJobFiles()`が起動時に実行され、`%TEMP%\whisper-mcp-jobs\`から7日以上経過した`.json`と`.log`ファイルを削除します。
- `check_config`がプライバシーモードの状態を報告するようになりました。
- 起動ログがプライバシーモードのオン/オフを報告します。
- `Job`インターフェースを`privacyMode: boolean`フィールドで拡張。
- `BatchState`インターフェースを`privacyMode: boolean`フィールドで拡張。
- `BackgroundFormat`型は`json`を除外します（バックグラウンドモードでのjsonは依然として非対応で、`text`にフォールバックします）。

### ✅ v2.4.0 — 堅牢化、フォアグラウンドガード、テストスイート＆CI

セキュリティ/堅牢性の見直し。予定されていたBun移行はv2.5.0へ移動しました。

**セキュリティと正確性：**
- `switch_model`のパス封じ込め修正 — 兄弟プレフィックスのディレクトリ（例：`…\models-evil`）が、以前は単純な`startsWith`によって「モデルディレクトリ内」チェックを満たし得ました。正規化された`relative()`ベースの封じ込めに置き換えました。SECURITY.mdが記述する脱出経路を塞ぎます。
- プライバシー/同意ゲートを**操作ごと**（ツール＋引数）にキー化 — ある文字起こしを確認しても、別の操作のゲートを満たせなくなりました。
- `download_model`は、`.part`ファイルを昇格させる前に、切り詰められたダウンロードを拒否します（Content-Lengthチェック）。（完全なSHA256ダイジェスト検証は後のパスで対応予定。）
- 入力の強制変換 — 実際の数値ではない数値型ツールパラメータは、`NaN`としてwhisper-cliに渡されるのではなく破棄されます。

**堅牢性：**
- **フォアグラウンドタイムアウトガード** — ブロッキングモードでClaude Desktopの約4分のMCPツールタイムアウトを超えるほど長いファイルを事前に検出し、サイレントにタイムアウトさせる代わりにバックグラウンドへルーティングします。閾値は`WHISPER_FOREGROUND_MAX_SEC`で設定可能です。時間推定を修正しました（旧GPU推定は大幅に過小予測していました。支配的なモデル再読み込みコストをモデル化しました — 推測ではなく実測です）。
- ジョブ/バッチ状態のアトミックな書き込み（一時ファイル＋リネーム）により、並行リーダーが破損したJSONファイルを観測できないようにしました。
- 衝突しないジョブ/バッチ/一時ID（UUIDサフィックス付き）。
- ブロッキングモードの一時ファイルをクリーンアップする、SIGINT/SIGTERMでの適切なシャットダウン。

**GPUデバイス選択：**
- `WHISPER_GPU_DEVICE`環境変数、および`gpu_device`が`generate_subtitles`と言語検出パスにも配線されました（以前は`transcribe_audio`のみ）。`check_config`はアクティブなデバイスを報告します。`check_system`は、`wmic`（Windows 11 24H2以降で非推奨）が何も返さない場合に、ドライバーの問題を誤報告しなくなりました。

**品質：**
- 純粋なロジック（パス封じ込め、ゲートのキー化、アトミック書き込み、入力の強制変換、タイムアウト推定）を対象とした`node:test`ユニットテストスイート。追加依存関係はゼロ。さらに、すべてのpush/PRで実行されるGitHub Actions CIワークフロー。

**将来のリリースに向けて特定：** 毎回の文字起こしで支払われるモデル再読み込みコストを排除する永続モデルパス（例：whisper.cppの`whisper-server`）— バッチ/アーカイブ作業における大きなスループット向上。

### ✅ v2.5.0 — 永続モデルサーバー + TinyDiarize

**永続モデルサーバー（フェーズ1）。** whisper-cliはワンショットです：呼び出しのたびにモデル全体を再読み込みします — v2.4.0では、メモリ制約のあるGPUでその再読み込みを約110秒と実測しました。これはファイルごとに支払う固定の税であり、バッチ/アーカイブ作業では実時間を支配します。v2.5.0は、文字起こしの間もモデルをメモリに保持するオプションの常駐モデルモードを追加します。
- `whisper_server`ツール（`start` / `stop` / `status`）。常駐サーバーが単一のインスタンス*となり*、1つのwhisperインスタンスというルールを保ちます：リクエストはそれに対して直列化され、並行性は導入されません。
- ブロッキングの`transcribe_audio`と`transcribe_batch`は、localhost（`127.0.0.1`）経由で`POST /inference`により常駐サーバーを通り、再読み込みコストを回避します。サーバーモードではフォアグラウンドタイムアウトガードはスキップされます（支払うべき再読み込みがないため）。
- `switch_model`は`POST /load`により再起動なしで常駐モデルをホットスワップします。`check_config`はサーバーの状態を報告します。所有するサーバーはVRAMを解放するためシャットダウン時にkillされます。
- 1エンジン / 共有VRAMのルールは、デタッチド生成パスのハードなバックストップと、丁寧な拒否とで強制されます：サーバー起動中は、バックグラウンドジョブ、`start_batch`、`generate_subtitles`、`lrc`/`csv`出力、およびHTTP APIが受け付けないper-requestオプション（`beam_size`、`best_of`、`word_timestamps`、`diarize`、`tinydiarize`、`vad_model`、`offset_t`、`duration`など）は、サイレントに劣化するのではなく「まずサーバーを停止してください」というメッセージとともに拒否されます。
- 設定：`WHISPER_SERVER_PATH`、`WHISPER_SERVER_PORT`（デフォルト8571、localhostのみ）。

**設計制約：**
- 明示的なライフサイクル：ヘルスチェックを伴う start / stop / status。無関係な呼び出しの副作用としてサーバーがサイレントに起動されることは決してありません。
- localhostのみにバインド — ルーティング可能なインターフェースには決してバインドしない。ネットワークへの公開なし（ローカルファーストの原則およびv2.4.0の堅牢化と一貫）。
- 適切なフォールバック：サーバーが実行されていない場合でも、既存のワンショットwhisper-cliパスを介して文字起こしは動作します。サーバーは最適化であり、必須の依存関係ではありません。
- `switch_model`は常駐サーバー内でモデルを再読み込みします（それでもファイルごとに再読み込みするより、償却すればはるかに安価です）。
- プライバシーと同意のゲートは変わりません — それらは文字起こしメカニズムの上位に位置します。
- 衝突処理付きのポート選択。既存の一時ファイルのクリーンアップと並行して、SIGINT/SIGTERMでのクリーンなシャットダウン。

**TinyDiarize。** `tdrz`対応モデルによる`--tinydiarize`サポート。ステレオ用の`--diarize`フラグ（v2.2.0）とは異なり、TinyDiarizeは**モノラル**録音で話者交代をマークし、モデルファイル以外には何も必要としません — Pythonも外部サービスも不要です。
- `transcribe_audio`と`generate_subtitles`の`tinydiarize`パラメータ（ブロッキングおよびバックグラウンドモード）。`--tinydiarize`を両方のarg builderに通しました。
- `download_model`が既存の信頼できるHugging Faceネームスペースから取得できるよう、`small.en-tdrz`を`MODEL_REGISTRY`に追加しました。

---

## 予定 — v2.6.0：永続モデルサーバー — フェーズ2

バックグラウンドジョブと`start_batch`を常駐サーバー経由でルーティングします。フェーズ1（v2.5.0）はブロッキング文字起こしのみを対象とします。こちらはより大きなアーカイブ/スループットの向上であり、デタッチドPIDではなくHTTPリクエストを中心にジョブ/キュー層を作り直す必要があります — PIDなしでの進捗追跡と、HTTPベースのキャンセルです。

v2.5.0で確立された常駐サーバーの**設計制約**は、フェーズ2でも引き続き支配します — localhostのみへのバインド、明示的なライフサイクル、適切なワンショットフォールバック、変わらないプライバシー/同意ゲート。フェーズ2は、これらのいずれも緩めることなく、ジョブ/キューのルーティングを追加します。

**状況：** 計画中。

---

## 予定 — v2.7.0：プロジェクト全体のトランスクリプト検索

プロジェクトディレクトリ内のすべてのトランスクリプトにわたってフレーズやパターンを検索し、一致箇所をそのソースファイルとタイムコードとともに返すスタンドアロンツール。より大きな動画プロジェクトワークフロー（「後日 / 検討中」を参照）から分解されたもの — こちらの半分は独立して有用であり、低リスクで、APIの使用も軽量です：検索はローカルで実行され、Claudeはユーザーが結果をレビューするときのみ関与します。

**状況：** 計画中。

---

## 予定 — v2.8.0：エディターにインポート可能な出力と連携フォーマット

トランスクリプトを、動画エディターが直接インポートするアーティファクトへと変換し、文字起こしがテキストファイルで止まるのではなく編集に供給されるようにします — これはプロジェクトの中核的な動機です：ソロクリエイターのために、大量の生素材アーカイブを扱いやすくすることです。

- **まずはマーカーCSV** — セグメントの開始点を、Premiere、Resolve、YouTubeがネイティブにインポートするマーカー/チャプターCSVとして出力します。完全なタイムラインフォーマットのコストとバージョン脆弱性のごく一部で、「エディターに取り込む」価値の大半を提供します。
- **単語レベルのタイミングデータ** — whisper.cppのフルトークンJSON（`--output-json-full` / `-ojf`）とDTWで整列された単語タイムスタンプ（`--dtw <preset>`、アクティブなモデルに自動マッチ。プリセットは`large.v3.turbo`を含むすべてのファミリーに存在し、量子化モデルにも適用されます）を公開します。これは、単語レベルSRT、マーカー配置、クリップ整列が土台とする正確なタイミング層です。トークンごとのJSONは、必要とする人向けに信頼度の値も保持します。注意：`--dtw`は**ロード時/コンテキストフラグ**（リクエストごとではなくモデル初期化時に設定）であるため、ワンショットのCLIパスに存在します — 常駐の`whisper-server`の`/inference` APIはこれをリクエストごとに適用できず、これはv2.5.0のサーバーモードにおける単語レベル拒否と整合します。
- **バックグラウンドでのJSONギャップを解消** — JSONは現在、バックグラウンドモードではテキストにフォールバックします。
- **FCPXML / EDL — 延期：** 冗長で、バージョンに敏感であり、エディター統合のスコープへ引き寄せます。マーカーCSVが不十分だと判明した場合にのみ再検討します。

**スコープの境界：** これはエディターが*インポート*するファイルを生成します — エディターのUIを自動化するものではありません。標準的なインターチェンジは設計思想に沿い、依存関係も軽量です。アプリケーションを駆動することは別個の関心事です。

v2.7.0と組み合わせて機能します：アーカイブを検索してその瞬間を見つけ、次にエディターにマーカーファイルを渡して、そこへ直接ジャンプします。

---

## 予定 — v2.9.0：文字起こしの品質とチューニング

文字起こしの精度と制御を深掘りします — いずれもラッパーがまだ公開していないwhisper.cppフラグの、依存関係ゼロのパススルーです。ここにあるすべてのオプションはワンショットの文字起こしパラメータです：ツール呼び出しのオーバーヘッドの追加はなく、フリープランのユーザーでも完全に機能します。

- **VADチューニング** — 音声区間検出のノブ（`--vad-threshold`、最小発話 / 最小無音 / 最大発話の長さ、speech-pad、samples-overlap）。VADはすでに有効ですがチューニング不可能です。これらは、現実世界の品質に関する不満の大半の背後にある過剰・過少なセグメンテーションを是正します。
- **非発話トークンの抑制**（`--suppress-nst`）— よりクリーンなトランスクリプトのために`[music]`/ノイズのアーティファクトを除去します。
- **言語検出のみ**（`--detect-language`）— 完全な文字起こしパスなしで返る、安価な「これは何語か？」プローブ。多言語のユーザー層にとって、また文字起こし前のルーティングにとって価値があります。
- **堅牢性 / デコード閾値** — 難しい音声向けの`--entropy-thold`、`--logprob-thold`、`--word-thold`、`--no-fallback`、`--temperature-inc`、`--carry-initial-prompt`、`--suppress-regex`。
- **パフォーマンスのノブ** — flash attention（現行のwhisper.cppでは**デフォルトで有効**。オプトインとして扱うのではなく、`--no-flash-attn` / `-nfa`による無効化パスを公開します）、CPU専用（`--no-gpu`）、オーディオコンテキストサイズ（`--audio-ctx`）。

**状況：** 計画中。

---

## 予定 — v3.0.0：字幕後処理スイート

サーバーがすでに出力するSRT / VTT / JSONの上に構築される、純粋なTypeScriptのバッチ層 — 再文字起こしなし、新しい依存関係なし、共有のパーサー/シリアライザー1つ。専用の字幕エディター（Subtitle Edit、Aegisub）の「バッチ変換」チェーンを模したもので、競合するどの文字起こしMCPも提供していないものです。とりわけタイミング修復パスは、生のWhisper出力が示す欠陥 — 無音区間の空キュー、重なり合うまたは短すぎるセグメント、リピートループの重複、長すぎる行 — を対象とするため、このスイートはインポートしたファイルだけでなく、このサーバー*自身*の出力もクリーンアップします。

- **タイミング修復と検証** — キューの最小/最大長を強制。重なるキューを修正。キュー間の最小ギャップを適用。閾値未満のギャップを橋渡し（次へ延長）。空キューを削除。重複キュー（whisperのリピートループ）をマージ。2行に制限。ソート＋再採番。加えて、選択可能なプロファイル（例：YouTube 42 CPL / 20 CPS、Netflix 42 / 17）に対して、キューごとの読み速度（CPS）、行あたり文字数、行数の違反をフラグする非変更の**lintレポート** — エディターがインポート前に実際に欲しがる成果物です。
- **リタイミング** — すべてのキューをオフセット/シフト。フレームレートのリタイミング（例：23.976 ↔ 25）。
- **リフロー** — 短いキューをマージ。長い行を最大の行あたり文字数 / 秒あたり文字数まで分割し、貪欲な分割ではなく2行のバランスを取ります。
- **フォーマット変換** — 既存のファイルをSRT / VTT / LRC / CSV / Markdown / プレーンの間で変換。加えて、再文字起こしなしでのASS/SSA出力（デフォルトスタイル付き）。書き込み時のUTF-8 / 改行コードの正規化（YouTubeのUTF-8要件を満たし、再インポート時の文字化けを防ぎます）。
- **テキストのクリーンアップ** — 検索/置換（regexはオプトイン）、静的なワードリストからのフィラーワード除去（LLMではない）、大文字小文字の正規化、聴覚障害者向け注釈の除去。厳密に機械的です — 判断を要するもの（OCRの修復、句読点の推論）は対象外とし、それらはホストのClaudeが返されたテキストに対して処理します。
- **話者ラベルの整形** — 既存のステレオ / TinyDiarizeの話者交代を、話者名を接頭辞に付けたブロックとして整形します。
- **サマリー統計** — 単語数、長さ、WPM、平均CPS、無音率。

**設計制約：**
- サーバーがすでに出力するSRT / VTT / JSONの上で動く純粋なTypeScript — 再文字起こしなし、新しいランタイム依存関係なし、共有のパーサー/シリアライザー1つ。
- 既存の字幕/トランスクリプトファイルに対してのみ動作 — whisperやffmpegを起動することは決してなく、音声に触れることも決してありません。
- 決定論的かつルールベースのみ — LLMなし、クラウドなし、「スマート」な修復なし。判断を要するもの（OCR修正、句読点の推論）は対象外とし、それらはホストのClaudeが返されたテキストに対して処理します。
- 非破壊的 — 新しいファイルを書き込みます。明示的なユーザー確認なしにソースファイルをその場で上書きすることは決してありません。
- lint / 検証パスは非変更です — 違反を報告するだけで、サイレントに書き換えることは決してありません。
- 標準的なインターチェンジフォーマットのみ — エディターのUIを駆動することは決してありません。

**状況：** 計画中。

---

## 後日 / 検討中

スケジュールされていませんが、設計思想に沿っており、余力に応じて再検討されます。

### Bunへの移行
ランタイムをNode.jsから[Bun](https://bun.sh)へ移行し、MCPサーバーのコールドスタート時間を削減し、`tsc`のビルドステップを廃止します（ソースが直接実行されます）。かつてのv2.5.0の枠から降格：呼び出しごとのモデル再読み込みコストが真のボトルネックである以上（上記v2.5.0を参照）、Nodeの起動を削るのは限界的な利得であり、Bun-on-Windowsの成熟度に加えて配信モデルの変更がリスクを伴います。いずれ行う価値のあるオプションの最適化であり、優先事項ではありません。

### 動画プロジェクトのリネーム＆マッチワークフロー
プロジェクトツールのより重い半分。プロジェクト全体のトランスクリプト検索（v2.7.0）が着地した後：編集済みクリップのトランスクリプトをソーストランスクリプトにファジーマッチしてソース内の位置を特定し、Claudeが提案する説明的なファイル名を表示します。

**設計制約：**
- ソースファイルは**決してリネームまたは変更しない**
- すべてのリネームには**明示的なユーザー確認**が必要
- 分析とマッチングはローカルで実行 — Claudeはユーザーが結果をレビューするときのみ呼び出され、API呼び出しを最小化します

**状況：** 設計フェーズ。

### ルールベースのトランスクリプトクリーンアップ
ローカルで決定論的な後処理 — フィラーワードと言い直しの除去、ユーザー制御。トランスクリプトがクリーンアップのためにClaudeに到達しないプライバシーモードのユーザーにとって最も価値があります。意図的に狭くしています：段落区切りとトピックのセグメンテーションは、返されたテキストに対してClaudeがすでに得意とすることであり、PDF/DOCXエクスポートはドキュメント生成へのスコープクリープです — どちらもここでは範囲外です。

**状況：** 昇格 — 決定論的なクリーンアップはv3.0.0の字幕後処理スイートに予定されています。スコープ外の注記（段落区切り、PDF/DOCX）は依然として有効です。

### 話者識別（pyannote-audio）
録音全体にわたる話者IDラベル付きの、完全なモノラル話者識別。組み込みのステレオ`--diarize`フラグ（v2.2.0）およびTinyDiarize（v2.5.0）とは異なります。

**実装：** [pyannote-audio](https://github.com/pyannote/pyannote-audio)が必要です — Hugging Faceのアクセストークンを要するPythonライブラリで、完全に別個の依存関係スタックです。優先度を下げました：ローカルファースト / 依存関係ゼロの設計思想と衝突し、TinyDiarizeがすでに依存関係ゼロのモノラルケースをカバーしているためです。もし追求する場合は、独自のセットアップドキュメントを備えたオプションの高度なアドオンとして提供され、メインパッケージに含まれることは決してありません。

**状況：** 優先度を下げた / オプション。

### 英語以外の言語への翻訳
Whisperの`--translate`フラグは英語のみを対象とします。任意のターゲット言語には、外部の翻訳APIまたはローカルの翻訳モデルが必要です。

**検討中のオプション：** LibreTranslate（セルフホスト可能、ローカルファースト）、ローカルLLMによる翻訳、または明示的にスコープ外とするドキュメント化。

**状況：** ローカルファーストかAPI依存かの判断待ちで保留中。

---

## スコープ外 / 計画なし

意図的に除外された機能。決定を明示的にし、繰り返し蒸し返されないようにここに記録します。

### ライブマイク文字起こし — 計画なし
ライブマイクからのリアルタイム文字起こしは、以前はv2.7.0に予定されていました。プロジェクトのコア設計と衝突するため取りやめました：
- **アーキテクチャの不一致：** MCPはストリーミングではなくリクエスト/レスポンスです。ライブキャプチャには、継続的なポーリング（APIの予算を消費する）か、v2.4.0のフォアグラウンドタイムアウトガードに引っかかる長時間ブロッキングの呼び出しのいずれかが必要になります。
- **1インスタンス / API最小化の原則：** ローリングセグメントをClaudeに返すことは絶え間ないツール呼び出しの回転であり — 「フリープランのユーザーでも機能する」の正反対です — また長寿命のストリーミングプロセスはプロセスロックに負担をかけます。
- **外部依存：** 追加の外部依存関係を必要とします。

ライブキャプション（低レイテンシ、デバイス管理、VAD）は、ファイル/バッチ文字起こしツールとは別個の製品カテゴリです。それを必要とするユーザーは、専用のリアルタイムツールでより良く対応されます。

### YouTube URL文字起こし（yt-dlp） — 同梱ツールとしては計画なし
yt-dlp経由のYouTubeからトランスクリプトへの直接変換は、以前は計画されていました。ファーストクラスの機能としては取りやめました。理由：
- **セキュリティ表面：** 任意URLの取得と、ユーザー制御の入力を伴うサブプロセス呼び出しを追加することになり、まさにその表面を削減したv2.4.0の堅牢化を逆行させます。
- **メンテナンス：** yt-dlpはYouTubeの変更に伴って頻繁に壊れます — 継続的なメンテナンスのコミットメントです。
- **ローカルファースト＆ライセンス：** ネットワークからのコンテンツ取得はローカルファーストのスコープの外にあり、ダウンローダーを商用ライセンスのプロジェクトに同梱することはToS/責任上のグレーゾーンです。
- **冗長：** ユーザーは自分でyt-dlpを実行し、その結果得られたファイルを`transcribe_audio`に指定できます。

**代替案：** 維持されるツールではなく、README / TROUBLESHOOTINGにレシピ（yt-dlpを実行してからファイルを文字起こしする）として文書化します — 依存関係や攻撃対象領域を抱えることなく、ワークフローは利用可能なまま保たれます。

---

## ライセンス

whisper-windows-mcpはデュアルライセンスを採用しています。

**非商用利用：** MIT — 個人・教育・非商用目的での利用は無償です。[LICENSE](LICENSE)をご参照ください。

**商用利用：** ビジネス・業務・収益を生む用途には別途商用ライセンスが必要です。条件と連絡先は[COMMERCIAL-LICENSE.md](COMMERCIAL-LICENSE.md)をご参照ください。

---

## 配信

[npm](https://www.npmjs.com/package/whisper-windows-mcp)、[mcpservers.org](https://mcpservers.org)、[Glama](https://glama.ai)、および[awesome-mcp-servers](https://github.com/punkpeye/awesome-mcp-servers)（PRサブミット済み）で公開中。

---

## 多言語ドキュメント

各リリース後に、英語ドキュメントに合わせて以下のファイルを更新する必要があります：

**日本語 (`*.ja.md`)** — `README.ja.md` / `TROUBLESHOOTING.ja.md` / `ROADMAP.ja.md` / `PRIVACY.ja.md` / `SECURITY.ja.md`

**韓国語 (`*.ko.md`)** — `README.ko.md` / `TROUBLESHOOTING.ko.md` / `ROADMAP.ko.md` / `PRIVACY.ko.md` / `SECURITY.ko.md`

**ベトナム語 (`*.vi.md`)** — `README.vi.md` / `TROUBLESHOOTING.vi.md` / `ROADMAP.vi.md` / `PRIVACY.vi.md` / `SECURITY.vi.md`

**インドネシア語 (`*.id.md`)** — `README.id.md` / `TROUBLESHOOTING.id.md` / `ROADMAP.id.md` / `PRIVACY.id.md` / `SECURITY.id.md`

**ウクライナ語 (`*.uk.md`)** — `README.uk.md` / `TROUBLESHOOTING.uk.md` / `ROADMAP.uk.md` / `PRIVACY.uk.md` / `SECURITY.uk.md`

**ブラジルポルトガル語 (`*.pt-BR.md`)** — `README.pt-BR.md` / `TROUBLESHOOTING.pt-BR.md` / `ROADMAP.pt-BR.md` / `PRIVACY.pt-BR.md` / `SECURITY.pt-BR.md`

**スペイン語 (`*.es.md`)** — `README.es.md` / `TROUBLESHOOTING.es.md` / `ROADMAP.es.md` / `PRIVACY.es.md` / `SECURITY.es.md`

**ポーランド語 (`*.pl.md`)** — `README.pl.md` / `TROUBLESHOOTING.pl.md` / `ROADMAP.pl.md` / `PRIVACY.pl.md` / `SECURITY.pl.md`

**ルーマニア語 (`*.ro.md`)** — `README.ro.md` / `TROUBLESHOOTING.ro.md` / `ROADMAP.ro.md` / `PRIVACY.ro.md` / `SECURITY.ro.md`

他の言語へのコミュニティ貢献を歓迎します。

---

## コントリビュート

プルリクエスト歓迎です。作業を開始する前に既存のIssueを確認してください。

上記に記載のないハードウェアでGPU加速をテストした方は、GPU型番、VRAM、モデルサイズ、確認したスループットを添えてIssueを立ててください。他のユーザーへの正確なパフォーマンス参考情報の構築に役立ちます。
