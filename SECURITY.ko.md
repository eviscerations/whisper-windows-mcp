# 보안 정책

## 범위

whisper-windows-mcp는 로컬 우선 도구입니다. 모든 음성 처리는 머신에서 이루어집니다 — 음성, 동영상 파일이나 개인 데이터가 어떤 서버에도 전송되지 않습니다. 공격 표면은 다음으로 제한됩니다:

- 로컬 파일시스템 (도구에 전달된 파일 경로)
- whisper-cli.exe 바이너리와 그 의존성
- Claude Desktop MCP 연결 (로컬 IPC만)
- 도구 응답에 반환된 전사 텍스트 (아래 개인 정보 아키텍처 참고)

## 개인 정보 아키텍처

**음성 파일은 절대 머신을 떠나지 않습니다.** 이 보장은 무조건적입니다.

**전사 텍스트는 기본 모드에서 머신을 떠날 수 있습니다.** 도구 응답에 전사 텍스트가 포함되면 해당 텍스트는 Claude의 API에서 처리됩니다. 이는 표준 MCP 동작이지만 규제 대상 또는 기밀 콘텐츠를 처리하는 사용자에게는 도구의 "로컬 우선" 설계 철학과 실제 데이터 흐름 사이에 격차를 만듭니다.

**개인 정보 모드** (`WHISPER_PRIVACY_MODE=true` 또는 호출별 `privacy_mode=true`)는 모든 도구 응답을 메타데이터만으로 제한합니다 — 전사 텍스트가 Claude의 API에 반환되지 않습니다. 이것이 의료, 법률, 재정, 기업 배포에 대한 올바른 설정입니다.

**개인 정보 모드 게이트:** 개인 정보 모드가 활성화된 경우 모든 전사 작업 전에 명시적 확인 공개가 작업별(도구 + 인수)로 키 지정되어 표시됩니다. 서버는 *차단*을 강제합니다 — 특정 작업을 처음 접하면 작업을 보류하고 공개를 반환합니다. 그러나 사람이 응답했는지는 강제하지 **않습니다**: 동일한 호출이 다시 발행되면, 호스트가 공개를 표시하고 사용자가 "예"라고 응답했다는 가정하에 게이트가 해제됩니다. 사람의 개입 없이 동일한 호출을 다시 발행하는 클라이언트는 스스로 게이트를 충족할 수 있습니다. 이것은 암호학적 장벽이 아니라 MCP 호스트가 공개를 준수하는 것에 의존하는 절차적 사전 동의 통제로 취급하세요.

**동의 게이트:** 기본 모드에서는 세션당 처음으로 전사 텍스트가 API에 반환되기 전에 일회성 세션 공개가 표시됩니다. 민감하지 않은 콘텐츠에 대해 이를 억제하려면 설정에서 `WHISPER_CONSENT_ACKNOWLEDGED=true`를 설정하세요. 이것은 *세션당 한 번* 게이트라는 점에 유의하세요: 첫 번째로 확인된 전사 이후, 동일 세션의 후속 전사는 다시 묻지 않고 반환됩니다. 세션 상태와 관계없이 절대 API에 도달해서는 안 되는 콘텐츠에는 개인 정보 모드를 사용하세요.

전체 개인 정보 아키텍처 설명, 컴플라이언스 프레임워크 안내(HIPAA, GDPR, 변호사-의뢰인 특권, FERPA, SOX, PCI-DSS) 및 설정 지침은 [PRIVACY.md](PRIVACY.md)를 참고하세요.

## 바이너리 검증

사전 빌드된 릴리스의 `whisper-cli.exe` 바이너리 무결성을 확인하려면 PowerShell에서 SHA256 해시를 확인하세요:

```powershell
Get-FileHash "C:\whisper\Release\whisper-cli.exe" -Algorithm SHA256
```

각 릴리스 바이너리에 대한 예상 해시는 [릴리스 페이지](https://github.com/eviscerations/whisper-windows-mcp/releases)에 게시됩니다. 해시가 일치하지 않는 바이너리는 사용하지 마세요.

## 지원되는 버전

보안 수정은 최신 게시된 버전에만 적용됩니다.

| 버전 | 지원됨 |
|---|---|
| 2.x (최신) | ✅ |
| 1.x | ❌ |

## 취약점 보고

**보안 취약점에 대해 공개 이슈를 열지 마세요.**

GitHub의 비공개 취약점 보고를 사용하세요:
1. [Security 탭](https://github.com/eviscerations/whisper-windows-mcp/security)으로 이동
2. "Report a vulnerability" 클릭
3. 재현할 수 있는 충분한 세부 정보로 문제를 설명

7일 이내에 응답을 받을 수 있습니다. 취약점이 확인되면 가능한 한 빨리 수정 사항을 릴리스하고 원하시면 릴리스 노트에 크레딧을 기재합니다.

## 샌드박스 및 승인

whisper-windows-mcp는 **Claude Desktop을 통해 머신 소유자가 구동하는 로컬, 단일 사용자 도구입니다.** 위협 모델은 소유자가 자신의 머신에서 실행하는 것이며 — 신뢰할 수 없거나, 멀티 테넌트이거나, 네트워크에 노출된 배포가 아닙니다.

- **샌드박싱:** 설계상 없습니다. `whisper-cli.exe`는 다른 로컬 MCP 서버와 마찬가지로 소유자 자신의 권한 수준에서 실행됩니다. 여기서의 완화책은 OS 수준 격리가 아니라 사용 범위입니다 — **이 서버를 신뢰할 수 없는 네트워크 접근에 노출하지 마세요**(아래 "파일 경로 인젝션" 참고).
- **승인은 샌드박스 기반이 아니라 계층적입니다:**
  1. **호스트 승인** — Claude Desktop의 MCP 레이어가 도구 호출을 제어합니다.
  2. **동의 / 개인 정보 게이트** — 전사 텍스트가 머신에서 Claude의 API로 나가기 전에 명시적 확인이 필요합니다. `WHISPER_PRIVACY_MODE` / 호출별 `privacy_mode`는 규제 대상 콘텐츠에 대해 메타데이터만 반환합니다. 게이트는 작업별(도구 + 인수)로 키 지정됩니다. [PRIVACY.md](PRIVACY.md)를 참고하세요.
  3. **입력 검증** — 경로나 ID를 받는 모든 도구에서 방어적으로 적용됩니다:
     - 디렉터리 트래버설(`..`) 및 UNC(`\\server\share`) 경로는 `analyze_media`와 `transcribe_batch`를 포함한 **모든** 파일/폴더 입력에서 거부됩니다(후자 둘은 이전에 존재 여부만 검증했습니다 — 검증되지 않은 UNC 경로가 공격자 호스트로의 아웃바운드 SMB 연결을 유발할 수 있었습니다).
     - `job_id` / `batch_id`는 파일시스템 경로를 구성하는 데 사용되기 전에 서버가 발급한 정확한 형식과 대조되므로, 조작된 ID가 작업 디렉터리 밖으로 탈출하여 임의 파일 읽기/쓰기/삭제로 이어질 수 없습니다.
     - `switch_model` **및** `transcribe_audio`의 `model` 재정의는 모두 정규화된 경로 격리를 통해 설정된 모델 디렉터리로 제한됩니다 — 재정의를 사용하여 임의의 파일을 `whisper-cli`에 모델로 전달할 수 없습니다.
     - `vad_model` 경로는 트래버설/UNC를 거부합니다.
     - `download_model`은 신뢰할 수 있는 Hugging Face 네임스페이스 허용 목록으로 제한됩니다(초기 URL 및 모든 리다이렉트).
     - 서버가 암묵적으로 호출하는 Windows 시스템 바이너리(`tasklist`, `wmic`)는 절대 `System32` 경로로 호출되므로 `PATH`상 앞에 심어진 동일 이름의 실행 파일로 섀도잉될 수 없습니다.

**"신뢰할 수 없는 에이전트" 경계에 대한 참고.** 이 도구는 공유 또는 네트워크 노출 인프라가 아니라, Claude Desktop을 통해 도구를 구동하는 단일 소유자를 위해 설계되었습니다. 그러나 전사된 음성/동영상 콘텐츠 자체는 신뢰할 수 없는 입력으로, *지시처럼 보일* 수 있으며 다음에 어떤 도구가 어떤 인수로 호출될지에 영향을 줄 수 있습니다(아래 "전사 인젝션" 참고). 그 때문에 위의 입력 검증은 단일 사용자 가정에만 의존하지 않고 방어적으로 적용됩니다. 완전히 신뢰할 수 없는 에이전트나 멀티 테넌트 태세는 여전히 OS/컨테이너 샌드박스와 이그레스 정책을 요구하며 — 로컬 단일 사용자 전사 도구의 범위를 벗어납니다.

## 알려진 설계 결정 사항

- **파일 경로 인젝션:** 도구는 Claude로부터 절대 파일 경로를 받습니다. 이것은 의도된 설계입니다 — 이 도구는 머신 소유자가 Claude Desktop과 함께 사용하도록 만들어졌습니다. 트래버설(`..`) 및 UNC 경로는 경로를 받는 모든 도구에서 거부됩니다. 그 외의 절대 로컬 경로는 허용됩니다. 이 MCP 서버를 신뢰할 수 없는 네트워크 접근에 노출하지 마세요.
- **작업/배치 ID 검증:** `job_id`와 `batch_id`는 파일시스템 경로를 구성하는 데 사용되기 전에 서버가 발급한 정확한 형식(`job_<epochMs>_<8 hex>` / `batch_<epochMs>_<8 hex>`)과 일치해야 합니다. 이는 조작된 ID가 작업 완료 처리 과정을 통해 작업 디렉터리 밖으로 탈출하여 임의 파일 읽기, 쓰기 또는 삭제로 이어지는 것을 방지합니다.
- **동의/개인 정보 게이트는 절차적입니다:** 게이트는 MCP 호스트가 공개를 표시하고 작업이 다시 발행되기 전에 사람이 응답하는 것에 의존합니다. 서버는 다시 발행될 때까지 차단하는 동작을 강제하지만 사람이 응답했는지는 검증할 수 없습니다. 절대 API에 도달해서는 안 되는 콘텐츠에는 게이트만이 아니라 개인 정보 모드(메타데이터만 반환)에 의존하세요.
- **샌드박싱 없음:** whisper-cli.exe는 Claude Desktop과 동일한 권한으로 실행됩니다. 이것은 로컬 MCP 도구의 표준 동작입니다.
- **임시 파일:** 중간 WAV 파일은 `%TEMP%\whisper_tmp_*.wav`에 작성되고 전사 후 삭제됩니다. 작업 상태 파일은 `%TEMP%\whisper-mcp-jobs\`에 작성되며 서버 시작 시 7일 후 자동으로 정리됩니다.
- **전사 콘텐츠:** 도구 응답에 반환된 전사 텍스트는 기본 모드에서 Claude의 API에서 처리됩니다. 이를 방지하려면 `WHISPER_PRIVACY_MODE=true`를 설정하거나 호출별로 `privacy_mode=true`를 전달하세요. [PRIVACY.md](PRIVACY.md)를 참고하세요.
- **전사 인젝션:** 음성 파일에는 전사 시 지시처럼 보이는 발화 내용이 포함될 수 있습니다. Claude의 내장 방어 기능이 이를 처리합니다. MCP 서버 자체는 모든 전사 콘텐츠를 신뢰할 수 없는 데이터로 표시하며 지시로 해석하지 않습니다.
- **모델 다운로드:** `download_model` 도구는 두 개의 신뢰할 수 있는 Hugging Face 네임스페이스(`ggerganov/whisper.cpp` 및 `ggml-org`)에서만 다운로드합니다. 리다이렉트는 허용 목록에 대해 검증된 후 따릅니다. 임의의 URL은 코드 수준에서 거부됩니다. 잘리거나 불완전한 다운로드는 `.part` 파일이 모델 이름으로 승격되기 전에 거부됩니다(Content-Length 검사). **후속 작업:** 다운로드는 아직 모델별 SHA256 다이제스트로 검증되지 않으므로, 손상된 업스트림이나 경로상의 공격자가 여전히 악성 `.bin`을 제공할 수 있습니다. 고정된 다이제스트가 계획되어 있습니다. 고신뢰 배포의 경우 릴리스 페이지의 해시를 수동으로 대조하여 검증하세요.
- **모델 선택 격리:** `switch_model`과 `transcribe_audio`의 `model` 재정의는 모두 설정된 모델 디렉터리 내의 `.bin` 파일만 허용합니다. 해당 디렉터리 외부의 경로는 정규화된 경로 격리를 통해 거부됩니다 — `…\models-evil`와 같은 형제 접두사 디렉터리는 검사를 통과할 수 없습니다 — 경로가 지정된 방식에 관계없이 거부됩니다. `vad_model` 경로는 트래버설/UNC를 거부합니다.
- **암묵적 시스템 바이너리:** `tasklist`와 `wmic`은 단순 이름이 아니라 절대 `System32` 경로로 호출되므로, `PATH`상 앞에 심어진 동일 이름의 실행 파일로 섀도잉될 수 없습니다.
- **영구 모델 서버:** 선택적 `whisper_server` 도구는 whisper.cpp의 `whisper-server`를 상주 프로세스로 실행합니다. 이것은 `127.0.0.1`에만 바인딩되어 — 라우팅 가능한 인터페이스에는 절대 바인딩되지 않으므로 — 머신 외부에서 접근할 수 없습니다. 명시적으로 시작 및 중지되며(자동 시작 없음), 소유한 프로세스는 종료 시 kill됩니다. 상주 서버와 일회성 `whisper-cli`는 동일한 GPU/VRAM을 두고 경합하므로 둘은 상호 배타적입니다: 분리된 생성 경로의 하드 백스톱이 서버 실행 중 어떤 CLI 작업도 시작되지 못하게 막고, 전사 도구는 서버가 중지될 때까지 CLI가 필요한 작업을 거부합니다. `WHISPER_SERVER_PORT`가 localhost 포트를 선택하며, 호스트는 설계상 구성할 수 없습니다.
