# whisper-windows-mcp — 로드맵

현재 버전: **v2.5.0**

---

## 설계 원칙

이 원칙들은 이 프로젝트의 모든 결정을 지배하며 기능 추가 속도보다 우선합니다.

**Claude API 사용 최소화.** 스캔, 분석, 큐 관리, 실행, 검증, 모델 전환에 이르는 전체 전사 워크플로우는 가능한 한 적은 수의 Claude 상호작용으로 실행되어야 합니다. 이 도구는 Pro나 Max 구독료를 지불하지 않는 무료 플랜 Claude 사용자도 완전히 사용할 수 있어야 합니다. 모든 도구 호출은 사용 예산을 소모합니다. 이 점을 고려해 설계하세요.

**항상 하나의 whisper 인스턴스.** 하나가 실행 중일 때 두 번째 whisper-cli.exe 프로세스를 절대 생성하지 마세요. 프로세스 잠금은 필수이며 타협의 여지가 없습니다.

**로컬 우선, 기본값은 개인 정보 보호.** 음성은 절대 머신을 벗어나지 않습니다. 핵심 기능에 클라우드 API가 필요하지 않습니다. 선택적 통합(예: Hugging Face 모델 다운로드)은 선택 사항임이 명확하게 문서화되어야 합니다.

**명시적인 사용자 제어.** 조용한 대량 작업은 없습니다. 파괴적이거나 되돌릴 수 없는 작업에는 확인이 필요합니다. 사용자는 어떤 일이 벌어지기 전에 항상 그것을 알고 있어야 합니다.

**Unicode 안전 경로.** 모든 파일 I/O는 일본어, 중국어, 이모지, 괄호 및 기타 특수 문자를 포함한 비ASCII 파일명을 올바르게 처리해야 합니다.

**모듈식이며 조합 가능.** 도구는 독립적입니다. 사용자는 필요한 것만 사용합니다. 불가피한 경우가 아니라면 어떤 기능도 다른 기능을 필요로 해서는 안 됩니다.

**기능보다 최적화 우선.** 기능을 추가할지 시스템 부하나 API 호출 수를 줄일지 망설여진다면 부하를 줄이세요. 대규모 최적화 작업은 비용이 큽니다. 처음부터 아키텍처를 제대로 잡으세요.

---

## 완료됨

### ✅ v1.3.1 — 프로세스 잠금
전사 생성 전에 `tasklist /FI`를 사용하는 `isWhisperRunning()` 확인 추가. 경쟁하는 프로세스를 생성하는 대신 작업 관리자 안내와 함께 명확한 오류를 반환합니다.

### ✅ v1.4.0 — Vulkan GPU 가속
VS Build Tools 2022와 Vulkan SDK를 사용하여 `-DGGML_VULKAN=ON`으로 whisper.cpp를 소스에서 컴파일. 사전 빌드된 Vulkan 바이너리를 `whisper-vulkan-win-x64.zip`으로 배포.

**AMD Radeon RX Vega 56에서의 결과:** 평균 GPU 사용률 약 16%. 58분짜리 파일이 GPU에서 약 4.5분에 완료되며, CPU 전용의 약 88분과 대비됩니다.

### ✅ v1.5.0 — 시스템 진단
`check_system` 도구: `wmic`을 통한 GPU 감지, Vulkan DLL 확인, VRAM 보고, 모델 크기 권장.

### ✅ v1.6.0 — 파일 사전 분석
FFprobe를 통한 `analyze_media` 도구: 재생 시간, 크기, 코덱, 전사 상태, CPU 및 GPU 시간 추정. 정렬 옵션이 있는 단일 파일 또는 폴더 스캔.

### ✅ v1.7.0 — 백그라운드 전사 + 진행 상황 가시성
분리형 프로세스 아키텍처: `background=true`인 `transcribe_audio`는 whisper를 분리된 프로세스로 생성하고 즉시 작업 ID를 반환합니다. `check_progress`는 whisper의 stderr 세그먼트 타임스탬프를 파싱하여 실시간 진행률과 ETA를 제공합니다.

### ✅ v1.8.0 — 검증이 포함된 순차 배치
`start_batch` 및 `check_batch_progress`: 자동 순차 처리, 전사 검증(빈/짧은 출력 감지), 자동 큐 진행, 파일별 진행 타임스탬프.

### ✅ v1.9.0 — 다국어 지원 및 번역
`language=auto` 감지와 `translate_to_english=true` 이중 SRT 출력을 지원하는 `generate_subtitles`. `.3gp` 및 `.ts` 포맷 지원 추가. `language=auto`는 `transcribe_audio`에서도 사용할 수 있습니다.

**알려진 제한:** Whisper의 내장 번역은 영어만 대상으로 합니다. 비영어 언어에는 `large-v3` 모델이 필요하며, 영어 전용 모델(`*.en.bin`)은 비영어 음성에 대해 `[FOREIGN]`을 출력합니다.

### ✅ v2.0.0 — Unicode 안전 경로 + 백그라운드 SRT
**Unicode 파일명:** 파일명에 비ASCII 문자가 있으면 백그라운드 전사가 조용히 실패하던 문제. 모든 출력을 정제된 작업 ID 기반 임시 경로로 라우팅한 뒤 완료 후 결과를 올바른 목적지로 이동하도록 하여 수정했습니다.

**백그라운드 모드의 SRT:** `spawnDetached`가 요청된 포맷과 무관하게 `-otxt`를 하드코딩하던 문제. `spawnDetached`에 `outputFormat` 파라미터를 추가하여 백그라운드 모드에서 `text` 및 `srt` 출력을 지원하도록 수정했습니다.

### ✅ v2.0.1 — 버그 수정 (v2.2.0에 포함되어 배포됨)
- `buildArgs`와 `spawnDetached` 양쪽에 `--max-context 0` 하드코딩 — 장시간 음성에서 환각 루프를 방지합니다.
- 두 함수 모두에 `--no-speech-thold 0.6` 하드코딩 — 신뢰도 임계값 미만인 세그먼트를 환각된 콘텐츠 대신 무음으로 처리합니다.
- 경로 검증(`validateInputPath`) — UNC 경로 및 `..` 탐색을 거부합니다.
- `MAX_FILE_SIZE_MB = 10240` 파일 크기 가드.
- `transcribeSingle`에 전사 인젝션 보안 주석 추가.
- TROUBLESHOOTING.md의 잘못된 CLI 배치 명령 수정.

### ✅ v2.1.0 — 모델 관리 스위트 (v2.2.0에 포함되어 배포됨)
- `WHISPER_MODEL`을 `const`에서 `let`으로 변경(세션 중 변경 가능).
- `MODEL_REGISTRY` — 16개 모델, 전체 정밀도 및 양자화 변형, Hugging Face 다운로드 URL.
- `ALLOWED_HF_PREFIXES` — 다운로드를 `ggerganov/whisper.cpp` 및 `ggml-org` 네임스페이스로 제한하는 URL 허용 목록.
- `list_models` 도구 — 모델 디렉터리를 스캔하여 활성 모델, 크기, 사용 사례, 사용 가능한 다운로드를 표시합니다.
- `download_model` 도구 — Node.js 내장 `https`를 통해 Hugging Face에서 다운로드하고 원자적 이름 변경을 수행합니다.
- `switch_model` 도구 — `.bin` 확장자 검증, 디렉터리 제약, 프로세스 잠금 확인.
- `recommendedModel()` 업데이트 — 6GB 이상 VRAM에 대해 `large-v3-turbo`를 권장합니다.

### ✅ v2.2.0 — 품질, 파라미터, 하드웨어 확장
- `buildArgs`의 위치 인수를 대체하는 `WhisperOptions` 인터페이스.
- `transcribe_audio`의 새 파라미터: `temperature`, `prompt`, `condition_on_prev_text`, `no_speech_thold`, `beam_size`, `best_of`, `gpu_device`, `processors`, `word_timestamps`, `max_segment_length`, `split_on_word`, `diarize`, `vad_model`, `offset_t`, `duration`.
- `generate_subtitles`의 새 파라미터: `temperature`, `prompt`, `beam_size`, `best_of`, `diarize`, `vad_model`.
- `spawnDetached` 리팩터링 — 백그라운드/배치 모드에서 모든 품질 플래그가 적용됩니다.
- 배치 출력 수정 — `readBatchProgress`가 이제 검증 전에 임시 출력을 최종 목적지로 이동합니다.

**플래그 호환성 참고:** `gpu_device` / `--device`는 whisper.cpp v1.8.4에서 추가되었습니다. 릴리스에 포함된 사전 빌드 Vulkan 바이너리는 v1.8.3 세대이므로, 이 파라미터는 도구에서 허용되지만 사용자가 v1.8.4 이상 바이너리로 업데이트하기 전까지는 효과가 없습니다.

### ✅ v2.2.2 — 패치
- 이중 라이선스 수정 — LICENSE 및 LICENSE-COMMERCIAL.md 교정.
- 사소한 문서 수정.

### ✅ v2.3.0 — 배치 자동 진행, 개인 정보 보호 아키텍처, 출력 포맷 확장

**배치 자동 진행(치명적 버그 수정):** `start_batch`는 이전에 큐를 진행하려면 능동적인 폴링이 필요했습니다. 이제 생성된 각 whisper-cli 자식 프로세스에 `on('exit')` 핸들러가 연결됩니다. 프로세스가 종료되면 exit 콜백을 통해 배치가 즉시 스스로 진행되며, 폴링 오버헤드와 API 호출 소모가 전혀 없습니다. 동시에 발생하는 exit 핸들러와 `check_batch_progress` 호출 간의 이중 생성은 뮤텍스로 방지합니다.

**개인 정보 보호 아키텍처:**
- `WHISPER_PRIVACY_MODE` 환경 변수 — `true`일 때 모든 도구 응답은 메타데이터(파일명, 단어 수, 저장 경로)만 반환합니다. 전사 텍스트는 절대 Claude API로 전송되지 않습니다. 전사본은 로컬 파일로만 존재합니다.
- `WHISPER_CONSENT_ACKNOWLEDGED` 환경 변수 — `true`일 때 민감하지 않은 콘텐츠에 대한 일회성 세션 동의 게이트를 억제합니다.
- `transcribe_audio`, `transcribe_batch`, `start_batch`, `check_progress`의 호출별 `privacy_mode` 파라미터. 전역 환경 변수를 양방향으로 재정의합니다. 호출별 토글에 재시작이 필요하지 않습니다.
- 개인 정보 보호 모드 게이트(`checkPrivacyGate()`) — 유효한 개인 정보 보호 모드가 활성화되어 있을 때 모든 작업 전에 실행됩니다. 첫 호출 시 활성화(공지 표시), 두 번째 호출 시 해제(허용). 각 작업 후 초기화됩니다. 세션 동의 게이트와는 완전히 독립적입니다.
- 세션 동의 게이트(`transcriptPolicy()`) — 표준 모드에서 첫 전사본 반환 호출 전에 세션당 한 번 실행됩니다. `sessionConsentGiven` 플래그로 소비됩니다.
- `PRIVACY.md` — HIPAA, GDPR, 변호사-의뢰인 특권, FERPA, SOX, PCI-DSS, NDA/영업 비밀을 다루는 전체 컴플라이언스 문서.
- 전사본을 반환하는 모든 도구의 도구 설명에 개인 정보 보호 경고 추가.

**출력 포맷 확장:**
- `vtt` — `-ovtt`를 통한 WebVTT 자막 출력. `transcribe_audio`, `generate_subtitles`, `start_batch`, 백그라운드 모드에서 사용 가능합니다.
- `lrc` — `-olrc`를 통한 LRC 가사/노래방 포맷. `transcribe_audio` 및 백그라운드 모드에서 사용 가능합니다.
- `csv` — `-ocsv`를 통한 타임스탬프 포함 CSV. `transcribe_audio` 및 백그라운드 모드에서 사용 가능합니다.
- `output_format` 기본값이 모든 도구와 코드 경로에서 `"text"`에서 `"timestamps"`로 변경됨. 이제 일반 텍스트는 옵트인입니다.

**버그 수정:**
- 버그 1: `output_format`이 백그라운드 작업으로 전달되지 않아, 요청된 포맷과 무관하게 기본 `"text"`가 사용되었습니다. 기본값을 `"timestamps"`로 변경하고 올바르게 전달하도록 수정했습니다.
- 버그 2: 백그라운드 작업의 출력 이동 작업에 있던 조용한 `catch {}`가 실패를 삼켰습니다. 이동 후 명시적인 `existsSync` 확인과 상세한 실패 메시지를 추가했습니다.
- 버그 3: 백그라운드 생성 지점에, 비개인 정보 백그라운드 작업의 경우 동의 게이트를 의도적으로 `check_progress`로 연기하는 이유를 설명하는 설계 주석을 추가했습니다.

**추가 사항:**
- 임시 디렉터리 자동 정리 — `cleanupOldJobFiles()`가 시작 시 실행되어 `%TEMP%\whisper-mcp-jobs\`에서 7일이 지난 `.json` 및 `.log` 파일을 삭제합니다.
- `check_config`가 이제 개인 정보 보호 모드 상태를 보고합니다.
- 시작 로그에 개인 정보 보호 모드의 켜짐/꺼짐이 보고됩니다.
- `Job` 인터페이스에 `privacyMode: boolean` 필드 추가.
- `BatchState` 인터페이스에 `privacyMode: boolean` 필드 추가.
- `BackgroundFormat` 타입에서 `json` 제외(백그라운드 모드의 json은 여전히 미지원 — `text`로 폴백).

### ✅ v2.4.0 — 강화, 포그라운드 가드, 테스트 스위트 및 CI

보안/견고성 점검 패스이며, 계획되어 있던 Bun 마이그레이션은 v2.5.0으로 이동했습니다.

**보안 및 정확성:**
- `switch_model` 경로 격리 수정 — 형제 접두사 디렉터리(예: `…\models-evil`)가 이전에는 단순한 `startsWith`로 "모델 디렉터리 내부" 검사를 통과할 수 있었으나, 정규화된 `relative()` 기반 격리로 교체했습니다. SECURITY.md가 설명하는 탈출 경로를 차단합니다.
- 개인 정보/동의 게이트를 **작업별**(도구 + 인수)로 키 지정 — 한 전사를 확인해도 더 이상 다른 작업의 게이트를 충족할 수 없습니다.
- `download_model`은 `.part` 파일을 승격하기 전에 잘린 다운로드를 거부합니다(Content-Length 검사). (전체 SHA256 다이제스트 검증은 이후 패스로 추적합니다.)
- 입력 강제 변환 — 실제 숫자가 아닌 숫자형 도구 파라미터는 `NaN`으로 whisper-cli에 넘어가는 대신 폐기됩니다.

**견고성:**
- **포그라운드 타임아웃 가드** — 차단 모드에서 Claude Desktop의 약 4분 MCP 도구 타임아웃을 초과할 만큼 긴 파일을 미리 감지하여 조용히 타임아웃되는 대신 백그라운드로 라우팅합니다. 임계값은 `WHISPER_FOREGROUND_MAX_SEC`로 구성할 수 있습니다. 시간 추정을 수정했습니다(기존 GPU 추정치는 심하게 과소평가했으며, 이제 지배적인 모델 재로드 비용을 모델링합니다 — 추측이 아닌 실측).
- 원자적인 작업/배치 상태 쓰기(임시 파일 + 이름 변경)로 동시 리더가 손상된 JSON 파일을 관찰할 수 없도록 했습니다.
- 충돌 방지 작업/배치/임시 ID(UUID 접미사).
- 차단 모드의 임시 파일을 정리하는 우아한 SIGINT/SIGTERM 종료.

**GPU 장치 선택:**
- `WHISPER_GPU_DEVICE` 환경 변수, 그리고 이제 `gpu_device`가 `generate_subtitles`와 언어 감지 패스에도 연결되었습니다(이전에는 `transcribe_audio`에만 적용). `check_config`가 활성 장치를 보고합니다. `check_system`은 이제 `wmic`(Windows 11 24H2 이상에서 더 이상 지원되지 않음)이 아무것도 반환하지 않을 때 더 이상 드라이버 문제를 잘못 보고하지 않습니다.

**품질:**
- 순수 로직(경로 격리, 게이트 키 지정, 원자적 쓰기, 입력 강제 변환, 타임아웃 추정)에 대한 `node:test` 단위 테스트 스위트를 추가 의존성 없이 구성했으며, 모든 push/PR에서 이를 실행하는 GitHub Actions CI 워크플로우도 포함합니다.

**향후 릴리스를 위해 식별됨:** 매 전사마다 발생하는 모델 재로드 비용을 제거하기 위한 영구 모델 경로(예: whisper.cpp의 `whisper-server`) — 배치/아카이브 작업에서 큰 처리량 향상.

### ✅ v2.5.0 — 영구 모델 서버 + TinyDiarize

**영구 모델 서버(1단계).** whisper-cli는 일회성입니다. 매 호출마다 전체 모델을 다시 로드하며, v2.4.0에서 메모리 제약이 있는 GPU에서 그 재로드가 약 110초로 측정되었습니다 — 배치/아카이브 작업의 실제 소요 시간(wall-clock)을 지배하는 파일당 고정 비용입니다. v2.5.0은 전사 사이에 모델을 메모리에 유지하는 선택적 상주 모델 모드를 추가합니다.
- `whisper_server` 도구(`start` / `stop` / `status`). 상주 서버가 *바로 그* 단일 인스턴스가 되어 하나의 whisper 인스턴스 규칙을 보존합니다. 요청은 이 서버에 대해 직렬화되며, 동시성은 도입되지 않습니다.
- 차단 모드의 `transcribe_audio`와 `transcribe_batch`는 localhost(`127.0.0.1`)를 통해 `POST /inference`로 상주 서버를 경유하여 재로드 비용을 건너뜁니다. 서버 모드에서는 포그라운드 타임아웃 가드를 건너뜁니다(지불할 재로드가 없음).
- `switch_model`은 재시작 없이 `POST /load`를 통해 상주 모델을 핫스왑합니다. `check_config`는 서버 상태를 보고하며, 소유한 서버는 VRAM 해제를 위해 종료 시 kill됩니다.
- 하나의 엔진 / 공유 VRAM 규칙은 분리형 생성 경로의 하드 백스톱과 친절한 거부로 강제됩니다: 서버가 실행 중인 동안 백그라운드 작업, `start_batch`, `generate_subtitles`, `lrc`/`csv` 출력, 그리고 HTTP API가 준수하지 않는 요청별 옵션(`beam_size`, `best_of`, `word_timestamps`, `diarize`, `tinydiarize`, `vad_model`, `offset_t`, `duration` 등)은 조용히 품질이 저하되는 대신 "먼저 서버를 중지하세요" 메시지와 함께 거부됩니다.
- 설정: `WHISPER_SERVER_PATH`, `WHISPER_SERVER_PORT`(기본값 8571, localhost 전용).

**설계 제약:**
- 명시적 수명 주기: 시작 / 중지 / 상태, 그리고 상태 확인. 서버는 관련 없는 호출의 부수 효과로 조용히 시작되지 않습니다.
- localhost에만 바인딩 — 라우팅 가능한 인터페이스에는 절대 바인딩하지 않음. 네트워크 노출 없음(로컬 우선 원칙 및 v2.4.0 강화와 일관).
- 우아한 폴백: 서버가 실행 중이지 않으면 기존의 일회성 whisper-cli 경로를 통해 전사가 계속 작동합니다. 서버는 최적화일 뿐 하드 의존성이 아닙니다.
- `switch_model`은 상주 서버에서 모델을 다시 로드합니다(그래도 파일마다 재로드하는 것보다 상각 기준으로 훨씬 저렴합니다).
- 개인 정보 및 동의 게이트는 변경되지 않습니다 — 전사 메커니즘 위에 위치합니다.
- 충돌 처리가 포함된 포트 선택; 기존 임시 파일 정리와 함께 SIGINT/SIGTERM에서의 깔끔한 종료.

**TinyDiarize.** `tdrz`가 활성화된 모델을 사용한 `--tinydiarize` 지원. 스테레오 `--diarize` 플래그(v2.2.0)와 달리 TinyDiarize는 **모노** 녹음에서 화자 전환을 표시하며, 모델 파일 외에는 아무것도 필요하지 않습니다 — Python도, 외부 서비스도 없습니다.
- `transcribe_audio`와 `generate_subtitles`의 `tinydiarize` 파라미터(차단 및 백그라운드 모드); `--tinydiarize`가 두 arg 빌더 모두에 연결됨.
- `download_model`이 기존의 신뢰할 수 있는 Hugging Face 네임스페이스에서 가져올 수 있도록 `MODEL_REGISTRY`에 `small.en-tdrz` 추가.

---

## 계획됨 — v2.6.0: 영구 모델 서버 — 2단계

백그라운드 작업과 `start_batch`를 상주 서버로 라우팅합니다. 1단계(v2.5.0)는 차단 전사만 다룹니다. 이것은 더 큰 아카이브/처리량 개선이며, 작업/큐 레이어를 분리형 PID 대신 HTTP 요청 중심으로 재작업해야 합니다 — PID 없는 진행 상황 추적, 그리고 HTTP 기반 취소.

v2.5.0에서 확립된 상주 서버 **설계 제약**은 2단계에서도 계속 지배합니다 — localhost 전용 바인딩, 명시적 수명 주기, 우아한 일회성 폴백, 그리고 변경되지 않는 개인 정보/동의 게이트. 2단계는 이들 중 어느 것도 완화하지 않으면서 작업/큐 라우팅을 추가합니다.

**상태:** 계획됨.

---

## 계획됨 — v2.7.0: 프로젝트 전체 전사본 검색

프로젝트 디렉터리 내 모든 전사본에서 구문이나 패턴을 검색하고, 소스 파일 및 타임코드와 함께 일치 항목을 반환하는 독립형 도구. 더 큰 동영상 프로젝트 워크플로우("나중에 / 검토 중" 참고)에서 분해된 것으로, 이 절반은 독립적으로 유용하고 위험이 낮으며 API 사용이 적습니다: 검색은 로컬에서 실행되고, Claude는 사용자가 결과를 검토할 때만 관여합니다.

**상태:** 계획됨.

---

## 계획됨 — v2.8.0: 에디터로 가져올 수 있는 출력 및 통합 포맷

전사본을 동영상 에디터가 직접 가져올 수 있는 산출물로 만들어, 전사가 텍스트 파일에서 멈추지 않고 편집으로 이어지게 합니다 — 이 프로젝트의 핵심 동기, 즉 방대한 원본 푸티지 아카이브를 1인 크리에이터가 다룰 수 있게 만드는 것입니다.

- **마커 CSV 우선** — 세그먼트 시작점을 Premiere, Resolve, YouTube가 기본으로 가져올 수 있는 마커/챕터 CSV로 제공합니다. 완전한 타임라인 포맷의 비용과 버전 취약성의 극히 일부로 "내 에디터로 가져오기" 가치의 대부분을 제공합니다.
- **단어 수준 타이밍 데이터** — whisper.cpp의 전체 토큰 JSON(`--output-json-full` / `-ojf`)과 DTW 정렬 단어 타임스탬프(`--dtw <preset>`, 활성 모델에 자동 매칭됨; `large.v3.turbo`를 포함한 모든 계열에 프리셋이 존재하며 양자화 모델에도 적용됨)를 노출합니다. 이것은 단어 수준 SRT, 마커 배치, 클립 정렬이 얹히는 정확한 타이밍 레이어이며, 토큰별 JSON은 원하는 사람을 위해 신뢰도 값도 함께 담습니다. 참고: `--dtw`는 **로드 시점/컨텍스트 플래그**(요청별이 아니라 모델 초기화 시 설정됨)이므로 일회성 CLI 경로에 속합니다 — 상주 `whisper-server`의 `/inference` API는 이를 요청별로 적용할 수 없으며, 이는 v2.5.0의 서버 모드 단어 수준 거부와 일관됩니다.
- **백그라운드 JSON 격차 해소** — JSON은 현재 백그라운드 모드에서 텍스트로 폴백됩니다.
- **FCPXML / EDL — 연기:** 장황하고 버전에 민감하며 에디터 통합 범위 쪽으로 끌어당깁니다. 마커 CSV가 불충분한 것으로 판명될 때만 재검토합니다.

**범위 경계:** 이것은 에디터가 *가져오는* 파일을 생성할 뿐, 에디터의 UI를 자동화하지는 않습니다. 표준 교환 포맷은 이 프로젝트의 철학에 부합하고 의존성이 가볍지만, 애플리케이션을 구동하는 것은 별개의 문제입니다.

v2.7.0과 짝을 이룹니다: 아카이브를 검색해 그 순간을 찾은 다음, 에디터에 마커 파일을 건네 곧바로 그 지점으로 이동합니다.

---

## 계획됨 — v2.9.0: 전사 품질 및 튜닝

전사 정확도와 제어에 대한 심화 — 모두 래퍼가 아직 노출하지 않은 whisper.cpp 플래그의 의존성 제로 패스스루입니다. 여기의 모든 옵션은 일회성 전사 파라미터입니다: 추가 도구 호출 오버헤드가 없고, 무료 플랜 사용자도 완전히 사용할 수 있습니다.

- **VAD 튜닝** — 음성 활동 감지 노브(`--vad-threshold`, 최소 발화 / 최소 무음 / 최대 발화 지속 시간, 발화 패딩, 샘플 오버랩). VAD는 이미 켜져 있지만 튜닝할 수 없으며, 이 노브들은 대부분의 실제 품질 불만 뒤에 있는 과분할과 과소분할 동작을 바로잡습니다.
- **비발화 토큰 억제**(`--suppress-nst`) — 더 깔끔한 전사본을 위해 `[music]` / 잡음 아티팩트를 제거합니다.
- **언어 감지 전용**(`--detect-language`) — 전체 전사 패스 없이 반환되는 저렴한 "이건 무슨 언어지?" 프로브. 다국어 사용자층에, 그리고 전사 전 라우팅에 유용합니다.
- **견고성 / 디코딩 임계값** — 어려운 음성을 위한 `--entropy-thold`, `--logprob-thold`, `--word-thold`, `--no-fallback`, `--temperature-inc`, `--carry-initial-prompt`, `--suppress-regex`.
- **성능 노브** — flash attention(현재 whisper.cpp에서 **기본 켜짐**이므로, 옵트인으로 취급하는 대신 `--no-flash-attn` / `-nfa` 비활성화 경로를 노출), CPU 전용(`--no-gpu`), 오디오 컨텍스트 크기(`--audio-ctx`).

**상태:** 계획됨.

---

## 계획됨 — v3.0.0: 자막 후처리 스위트

서버가 이미 내보내는 SRT / VTT / JSON 위에 놓이는 순수 TypeScript 배치 레이어 — 재전사도, 새 의존성도 없으며, 하나의 공유 파서/시리얼라이저를 사용합니다. 어떤 경쟁 전사 MCP도 제공하지 않는, 전용 자막 편집기(Subtitle Edit, Aegisub)의 "일괄 변환" 체인을 반영합니다. 특히 타이밍 복구 패스는 원본 Whisper 출력이 보이는 결함 — 무음 구간의 빈 큐, 겹치거나 너무 짧은 세그먼트, 반복 루프 중복, 지나치게 긴 줄 — 을 겨냥하므로, 이 스위트는 가져온 파일뿐 아니라 이 서버 *자신의* 출력도 정리합니다.

- **타이밍 복구 및 검증** — 최소 / 최대 큐 지속 시간 강제; 겹치는 큐 수정; 최소 큐 간 간격 적용; 임계값 미만 간격 연결(다음까지 확장); 빈 큐 제거; 중복 큐 병합(whisper 반복 루프); 최대 두 줄로 제한; 정렬 + 재번호 부여. 여기에 더해, 선택 가능한 프로필(예: YouTube 42 CPL / 20 CPS, Netflix 42 / 17)에 대해 큐별 읽기 속도(CPS), 줄당 글자 수, 줄 수 위반을 표시하는 비변형 **린트 리포트** — 에디터가 가져오기 전에 실제로 원하는 산출물입니다.
- **재타이밍** — 모든 큐를 오프셋 / 이동; 프레임 레이트 재타이밍(예: 23.976 ↔ 25).
- **리플로우** — 짧은 큐 병합; 긴 줄을 최대 줄당 글자 수 / 초당 글자 수로 분할하되, 탐욕적 분할 대신 두 줄의 균형을 맞춤.
- **포맷 변환** — 재전사 없이 기존 파일을 SRT / VTT / LRC / CSV / Markdown / 일반 텍스트 사이에서 변환하고, ASS/SSA 출력(기본 스타일 적용)도 지원. 쓰기 시 UTF-8 / 줄 끝 정규화(YouTube의 UTF-8 요구 사항을 충족하고, 재가져오기 시 문자 깨짐을 방지).
- **텍스트 정리** — 찾기/바꾸기(정규식 옵트인), 정적 단어 목록 기반 필러 단어 제거(LLM 아님), 대소문자 정규화, 청각 장애인용 주석 제거. 엄격히 기계적이며, 판단이 필요한 것(OCR 복구, 문장 부호 추론)은 제외됩니다 — 그런 것은 호스트 Claude가 반환된 텍스트에서 처리합니다.
- **화자 레이블 포맷팅** — 기존 스테레오 / TinyDiarize 전환을 화자 접두사 블록으로 포맷합니다.
- **요약 통계** — 단어 수, 지속 시간, WPM, 평균 CPS, 무음 비율.

**설계 제약:**
- 서버가 이미 내보내는 SRT / VTT / JSON 위의 순수 TypeScript — 재전사도, 새 런타임 의존성도 없으며, 하나의 공유 파서/시리얼라이저.
- 기존 자막/전사본 파일에서만 작동 — whisper나 ffmpeg를 절대 호출하지 않고, 오디오를 절대 건드리지 않습니다.
- 결정적이고 규칙 기반일 뿐 — LLM도, 클라우드도, "스마트" 복구도 없습니다. 판단이 필요한 것(OCR 수정, 문장 부호 추론)은 제외되며, 호스트 Claude가 반환된 텍스트에서 처리합니다.
- 비파괴적 — 새 파일을 쓰며, 명시적인 사용자 확인 없이는 소스 파일을 제자리에서 덮어쓰지 않습니다.
- 린트 / 검증 패스는 비변형입니다 — 위반을 보고할 뿐, 절대 조용히 다시 쓰지 않습니다.
- 표준 교환 포맷만 — 에디터의 UI를 절대 구동하지 않습니다.

**상태:** 계획됨.

---

## 나중에 / 검토 중

일정이 잡히지 않았지만 철학에 부합하며 여력이 될 때 다시 검토합니다.

### Bun 마이그레이션
MCP 서버 콜드 스타트 시간을 줄이고 `tsc` 빌드 단계를 제거(소스를 직접 실행)하기 위해 런타임을 Node.js에서 [Bun](https://bun.sh)으로 마이그레이션. 기존 v2.5.0 자리에서 강등됨: 실제 병목이 호출별 모델 재로드 비용인 상황에서(위 v2.5.0 참고) Node의 시작 시간을 줄이는 것은 한계적 이득이며, Windows에서의 Bun 성숙도와 배포 모델 변경이 위험을 수반합니다. 우선순위가 아닌 선택적 최적화로서 언젠가 할 가치가 있습니다.

### 동영상 프로젝트 이름 변경 및 매칭 워크플로우
프로젝트 도구의 더 무거운 절반으로, 프로젝트 전체 전사본 검색(v2.7.0)이 완료된 후: 편집된 클립 전사본을 소스 전사본과 퍼지 매칭하여 원본 지점을 찾고, Claude가 제안한 설명적 파일명을 표시합니다.

**설계 제약:**
- 소스 파일은 **절대 이름 변경 또는 수정되지 않음**
- 모든 이름 변경에는 **명시적인 사용자 확인**이 필요
- 분석 및 매칭은 로컬에서 이루어짐 — Claude는 사용자가 결과를 검토할 때만 호출되어 API 호출을 최소화

**상태:** 설계 단계.

### 규칙 기반 전사본 정리
로컬의 결정적 후처리 — 필러 단어 및 말 막힘 제거, 사용자 제어. 정리를 위해 전사본이 Claude에 도달하지 않는 개인 정보 보호 모드 사용자에게 가장 유용합니다. 의도적으로 좁게 설정됨: 단락 구분과 주제 분할은 Claude가 반환된 텍스트에서 이미 잘 수행하는 것이고, PDF/DOCX 내보내기는 문서 생성 영역으로의 범위 확대입니다 — 둘 다 여기서는 범위 외입니다.

**상태:** 승격됨 — 결정적 정리는 v3.0.0 자막 후처리 스위트에 예정되어 있으며, 범위 외 항목(단락 구분, PDF/DOCX)에 대한 참고는 여전히 유효합니다.

### 화자 분리 (pyannote-audio)
전체 녹음에 걸쳐 화자 ID 레이블이 있는 완전한 모노 화자 분리. 내장 스테레오 `--diarize` 플래그(v2.2.0) 및 TinyDiarize(v2.5.0)와는 다릅니다.

**구현:** [pyannote-audio](https://github.com/pyannote/pyannote-audio)가 필요 — Hugging Face 접근 토큰이 필요한 Python 라이브러리로, 완전히 별개의 의존성 스택. 우선순위 하향됨: 로컬 우선 / 의존성 제로 철학과 충돌하며, TinyDiarize가 이미 의존성 제로의 모노 사례를 다룹니다. 추진한다면 자체 설정 문서를 갖춘 선택적 고급 애드온으로 제공되며, 절대 메인 패키지에 포함되지 않습니다.

**상태:** 우선순위 하향 / 선택적.

### 비영어 언어로의 번역
Whisper의 `--translate` 플래그는 영어만 대상으로 합니다. 임의의 대상 언어에는 외부 번역 API 또는 로컬 번역 모델이 필요합니다.

**검토 중인 옵션:** LibreTranslate(자체 호스팅 가능, 로컬 우선), 로컬 LLM 번역, 또는 명시적인 범위 외 문서화.

**상태:** 로컬 우선 대 API 의존성 결정 대기 중 연기됨.

---

## 범위 외 / 계획되지 않음

의도적으로 제외된 기능들로, 결정이 명시적으로 남아 반복적으로 재부상하지 않도록 여기에 기록합니다.

### 실시간 마이크 전사 — 계획되지 않음
라이브 마이크에서의 실시간 전사는 이전에 v2.7.0으로 예정되어 있었습니다. 프로젝트의 핵심 설계와 충돌하여 제외됨:
- **아키텍처 불일치:** MCP는 스트리밍이 아니라 요청/응답입니다. 라이브 캡처는 지속적인 폴링(API 예산 소모) 또는 v2.4.0 포그라운드 타임아웃 가드에 걸리는 장시간 차단 호출 중 하나를 요구합니다.
- **하나의 인스턴스 / API 최소화 원칙:** 롤링 세그먼트를 Claude에 반환하는 것은 끊임없는 도구 호출 발생 — "무료 플랜 사용자도 사용 가능"의 정반대 — 이며, 장기 실행 스트리밍 프로세스는 프로세스 잠금에 부담을 줍니다.
- **외부 의존성:** 추가적인 외부 의존성이 필요합니다.

라이브 자막(낮은 지연 시간, 장치 관리, VAD)은 파일/배치 전사 도구와는 별개의 제품 범주입니다. 이것이 필요한 사용자는 전용 실시간 도구로 더 잘 지원됩니다.

### YouTube URL 전사 (yt-dlp) — 번들 도구로 계획되지 않음
yt-dlp를 통한 YouTube에서 전사본으로의 직접 변환은 이전에 계획되어 있었습니다. 다음 이유로 일급 기능에서 제외됨:
- **보안 표면:** 임의 URL 페칭과 사용자 제어 입력이 있는 하위 프로세스 호출을 추가하여, 바로 그 표면을 줄였던 v2.4.0 강화를 되돌립니다.
- **유지 관리:** yt-dlp는 YouTube 변경에 따라 자주 깨집니다 — 지속적인 유지 관리 부담.
- **로컬 우선 & 라이선싱:** 네트워크 콘텐츠 획득은 로컬 우선 범위 밖에 있으며, 다운로더를 상업 라이선스 프로젝트에 번들하는 것은 ToS/책임의 회색 지대입니다.
- **중복:** 사용자는 yt-dlp를 직접 실행하고 결과 파일을 `transcribe_audio`에 지정할 수 있습니다.

**대안:** 유지 관리하는 도구가 아니라 레시피(yt-dlp를 실행한 뒤 파일을 전사)로서 README / TROUBLESHOOTING에 문서화됨 — 의존성이나 공격 표면을 소유하지 않고도 워크플로우는 계속 사용 가능합니다.

---

## 라이선싱

whisper-windows-mcp는 이중 라이선스를 적용합니다.

**비상업적 사용:** MIT — 개인, 교육, 비상업적 목적의 사용은 무료입니다. [LICENSE](LICENSE)를 참조하세요.

**상업적 사용:** 비즈니스, 전문적 또는 수익 창출 목적의 사용에는 별도의 상업용 라이선스가 필요합니다. 조건 및 연락처 정보는 [COMMERCIAL-LICENSE.md](COMMERCIAL-LICENSE.md)를 참조하세요.

---

## 배포

[npm](https://www.npmjs.com/package/whisper-windows-mcp), [mcpservers.org](https://mcpservers.org), [Glama](https://glama.ai), [awesome-mcp-servers](https://github.com/punkpeye/awesome-mcp-servers)에서 사용 가능(PR 제출됨).

---

## 다국어 문서

각 릴리스 후 다음 파일들을 영어 문서에 맞게 업데이트해야 합니다:

**일본어 (`*.ja.md`)** — `README.ja.md` / `TROUBLESHOOTING.ja.md` / `ROADMAP.ja.md` / `PRIVACY.ja.md` / `SECURITY.ja.md`

**한국어 (`*.ko.md`)** — `README.ko.md` / `TROUBLESHOOTING.ko.md` / `ROADMAP.ko.md` / `PRIVACY.ko.md` / `SECURITY.ko.md`

**베트남어 (`*.vi.md`)** — `README.vi.md` / `TROUBLESHOOTING.vi.md` / `ROADMAP.vi.md` / `PRIVACY.vi.md` / `SECURITY.vi.md`

**인도네시아어 (`*.id.md`)** — `README.id.md` / `TROUBLESHOOTING.id.md` / `ROADMAP.id.md` / `PRIVACY.id.md` / `SECURITY.id.md`

**우크라이나어 (`*.uk.md`)** — `README.uk.md` / `TROUBLESHOOTING.uk.md` / `ROADMAP.uk.md` / `PRIVACY.uk.md` / `SECURITY.uk.md`

**브라질 포르투갈어 (`*.pt-BR.md`)** — `README.pt-BR.md` / `TROUBLESHOOTING.pt-BR.md` / `ROADMAP.pt-BR.md` / `PRIVACY.pt-BR.md` / `SECURITY.pt-BR.md`

**스페인어 (`*.es.md`)** — `README.es.md` / `TROUBLESHOOTING.es.md` / `ROADMAP.es.md` / `PRIVACY.es.md` / `SECURITY.es.md`

**폴란드어 (`*.pl.md`)** — `README.pl.md` / `TROUBLESHOOTING.pl.md` / `ROADMAP.pl.md` / `PRIVACY.pl.md` / `SECURITY.pl.md`

**루마니아어 (`*.ro.md`)** — `README.ro.md` / `TROUBLESHOOTING.ro.md` / `ROADMAP.ro.md` / `PRIVACY.ro.md` / `SECURITY.ro.md`

다른 언어로의 커뮤니티 기여를 환영합니다.

---

## 기여

풀 리퀘스트를 환영합니다. 작업을 시작하기 전에 기존 이슈를 확인하세요.

위에 나열되지 않은 하드웨어에서 GPU 가속을 테스트했다면 GPU 모델, VRAM, 모델 크기, 관찰된 처리량을 이슈로 보고해 주세요. 이것은 다른 사용자를 위한 정확한 성능 참고 자료를 구축하는 데 도움이 됩니다.
